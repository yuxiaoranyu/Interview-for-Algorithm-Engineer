# 滴滴高频面试题

> 用于持续沉淀出行调度、供需预测、路径规划、地图、风控和机器学习工程相关面试题。

## 待补充方向

本文件作为该公司面试题的持续更新入口。后续可按照“岗位/面试轮次/问题/答案/手撕代码/复盘要点”的格式补充内容。

建议优先补充以下方向：

- 大模型、AIGC、多模态、Agent 或推荐搜索相关岗位面经。
- 公司核心业务场景中的算法工程问题。
- 高频手撕代码、系统设计、训练/推理/部署工程题。

## 目录

- [1. 滴滴暑期大模型算法一面凉经分享](#batch2-p1)
  - [1.lora和全量的显存计算怎么算，一般来说哪一部分占比最大](#batch2-p1-q1)
  - [2.描述一下dpo](#batch2-p1-q2)
  - [3.dpo的损失函数](#batch2-p1-q3)
  - [4.dpo训练参数](#batch2-p1-q4)
  - [5.（思考题）蒙特卡洛算法，先验概率](#batch2-p1-q5)
  - [6.7b模型用几张卡训](#batch2-p1-q6)
  - [7.llm as judge的偏见分为哪些，有什么解决方法](#batch2-p1-q7)
  - [8.rag流程](#batch2-p1-q8)
  - [9.假如关闭llm的think模式，但在prompt里依旧写<think>……</think>会怎样](#batch2-p1-q9)
  - [10.强化学习训练指标](#batch2-p1-q10)
  - [11.举一个自己的reward hawking例子](#batch2-p1-q11)
  - [12.rag的演变历程](#batch2-p1-q12)
  - [13.怎样算是一个优秀的prompt](#batch2-p1-q13)
  - [手撕:](#batch2-p1-q14)

<a id="batch2-p1"></a>
### 1. 滴滴暑期大模型算法一面凉经分享

#### 面试问题汇总

<a id="batch2-p1-q1"></a>
##### 1.lora和全量的显存计算怎么算，一般来说哪一部分占比最大

**回答：**
LoRA 冻结原模型权重，只训练低秩增量矩阵 ΔW=BA，从而降低显存和训练成本。适合数据量有限、需要快速适配垂直任务或多租户部署的场景。r 越大表达能力越强，但参数、显存和过拟合风险也越高；选择时要结合任务复杂度、数据量、target modules、alpha 和 dropout 一起调。

<a id="batch2-p1-q2"></a>
##### 2.描述一下dpo

**回答：**
PPO 通过 clipped policy objective 控制策略更新幅度，通常配合 reward model、reference model 和 critic；critic 通过回归 return/value target 来估计优势。DPO 直接用 chosen/rejected 偏好对优化策略，不显式训练 reward model 和 critic，工程更简单但受离线数据覆盖限制。GRPO 用同一 prompt 的多条采样做组内相对优势估计，减少 critic 成本，适合大模型 RL，但依赖 reward 区分度和采样质量。

<a id="batch2-p1-q3"></a>
##### 3.dpo的损失函数

**回答：**
PPO 通过 clipped policy objective 控制策略更新幅度，通常配合 reward model、reference model 和 critic；critic 通过回归 return/value target 来估计优势。DPO 直接用 chosen/rejected 偏好对优化策略，不显式训练 reward model 和 critic，工程更简单但受离线数据覆盖限制。GRPO 用同一 prompt 的多条采样做组内相对优势估计，减少 critic 成本，适合大模型 RL，但依赖 reward 区分度和采样质量。

<a id="batch2-p1-q4"></a>
##### 4.dpo训练参数

**回答：**
PPO 通过 clipped policy objective 控制策略更新幅度，通常配合 reward model、reference model 和 critic；critic 通过回归 return/value target 来估计优势。DPO 直接用 chosen/rejected 偏好对优化策略，不显式训练 reward model 和 critic，工程更简单但受离线数据覆盖限制。GRPO 用同一 prompt 的多条采样做组内相对优势估计，减少 critic 成本，适合大模型 RL，但依赖 reward 区分度和采样质量。

<a id="batch2-p1-q5"></a>
##### 5.（思考题）蒙特卡洛算法，先验概率

**回答：**
这类问题要先回答核心定义，再讲为什么需要它、如何实现、适用边界和工程风险。面试中不要只背概念，要结合大模型算法岗常见场景：数据构建、训练稳定性、推理成本、评估体系、线上鲁棒性和业务指标，给出可落地的判断。

<a id="batch2-p1-q6"></a>
##### 6.7b模型用几张卡训

**回答：**
这类问题要先回答核心定义，再讲为什么需要它、如何实现、适用边界和工程风险。面试中不要只背概念，要结合大模型算法岗常见场景：数据构建、训练稳定性、推理成本、评估体系、线上鲁棒性和业务指标，给出可落地的判断。

<a id="batch2-p1-q7"></a>
##### 7.llm as judge的偏见分为哪些，有什么解决方法

**回答：**
这类问题要先回答核心定义，再讲为什么需要它、如何实现、适用边界和工程风险。面试中不要只背概念，要结合大模型算法岗常见场景：数据构建、训练稳定性、推理成本、评估体系、线上鲁棒性和业务指标，给出可落地的判断。

<a id="batch2-p1-q8"></a>
##### 8.rag流程

**回答：**
RAG 的关键链路是切片、向量化、召回、rerank、上下文组装和带引用生成。稀疏检索适合关键词、实体和精确匹配，稠密检索适合语义泛化，实际常用 hybrid retrieval。Rerank 用更强的交叉编码匹配弥补向量召回排序不准。HyDE 先生成假想答案再检索，适合模糊问题，但要用 rerank 和引用校验防止生成内容带偏检索。

<a id="batch2-p1-q9"></a>
##### 9.假如关闭llm的think模式，但在prompt里依旧写<think>……</think>会怎样

**回答：**
这类问题要先回答核心定义，再讲为什么需要它、如何实现、适用边界和工程风险。面试中不要只背概念，要结合大模型算法岗常见场景：数据构建、训练稳定性、推理成本、评估体系、线上鲁棒性和业务指标，给出可落地的判断。

<a id="batch2-p1-q10"></a>
##### 10.强化学习训练指标

**回答：**
这类问题要先回答核心定义，再讲为什么需要它、如何实现、适用边界和工程风险。面试中不要只背概念，要结合大模型算法岗常见场景：数据构建、训练稳定性、推理成本、评估体系、线上鲁棒性和业务指标，给出可落地的判断。

<a id="batch2-p1-q11"></a>
##### 11.举一个自己的reward hawking例子

**回答：**
Reward 设计要拆成正确性、指令遵循、安全性、事实一致性、工具调用质量等多维信号。规则 reward 适合格式、单测、引用、检索命中等可验证目标；模型 reward 适合语义质量和复杂偏好。避免 reward hacking 要使用多维 reward、KL 约束、hard negative、人工抽检、reward model ensemble，并监控长度、拒答率、幻觉率和多样性等副指标。

<a id="batch2-p1-q12"></a>
##### 12.rag的演变历程

**回答：**
RAG 的关键链路是切片、向量化、召回、rerank、上下文组装和带引用生成。稀疏检索适合关键词、实体和精确匹配，稠密检索适合语义泛化，实际常用 hybrid retrieval。Rerank 用更强的交叉编码匹配弥补向量召回排序不准。HyDE 先生成假想答案再检索，适合模糊问题，但要用 rerank 和引用校验防止生成内容带偏检索。

<a id="batch2-p1-q13"></a>
##### 13.怎样算是一个优秀的prompt

**回答：**
这类问题要先回答核心定义，再讲为什么需要它、如何实现、适用边界和工程风险。面试中不要只背概念，要结合大模型算法岗常见场景：数据构建、训练稳定性、推理成本、评估体系、线上鲁棒性和业务指标，给出可落地的判断。

<a id="batch2-p1-q14"></a>
##### 手撕:

**回答：**
手撕题要先明确输入输出和复杂度，再写稳健实现。链表题注意 dummy/head、空指针和边界；第 K 大可用大小为 K 的最小堆或 quickselect；shuffle 要用 Fisher-Yates，保证每个排列等概率；k-means 要说明初始化、分配、更新、收敛条件和空簇处理。面试中代码可简洁，但复杂度、边界和测试样例必须讲清楚。
