# 🏗️ 第二章：AI 视频核心基础架构模型

> 💡 **本节目标**：系统梳理 AI 视频生成领域的核心架构、关键组件与工程选型，帮助面试者理解主流视频生成模型的设计思想。

---

# 目录导航

[1.文本与多模态条件编码器：T5、CLIP 及条件注入](#1.文本与多模态条件编码器t5clip及条件注入)
  - [面试问题：为什么 AI 视频模型里经常会出现 T5 和 CLIP？](#面试问题为什么-ai-视频模型里经常会出现-t5-和-clip)
  - [面试问题：T5 和 CLIP 的核心差异是什么？](#面试问题t5-和-clip-的核心差异是什么)
  - [面试问题：为什么有些模型要同时使用 T5 和 CLIP？](#面试问题为什么有些模型要同时使用-t5-和-clip)
  - [面试问题：条件信息通常是如何注入到视频生成模型里的？](#面试问题条件信息通常是如何注入到视频生成模型里的)
  - [面试问题：Cross-Attention 为什么在视频生成里很重要？](#面试问题cross-attention-为什么在视频生成里很重要)

[2.VAE、LDM 与潜空间建模](#2.vaeldm与潜空间建模)
  - [面试问题：什么是 VAE？它在 AI 视频里有什么作用？](#面试问题什么是-vae它在-ai-视频里有什么作用)
  - [面试问题：为什么视频扩散模型通常要在潜空间建模？](#面试问题为什么视频扩散模型通常要在潜空间建模)
  - [面试问题：VAE 的 KL Loss 是干什么的？](#面试问题vae-的-kl-loss-是干什么的)
  - [面试问题：为什么说 VAE 会带来信息损失？](#面试问题为什么说-vae-会带来信息损失)
  - [面试问题：视频 VAE 和图像 VAE 有什么不同？](#面试问题视频-vae-和图像-vae-有什么不同)

[3.Video Diffusion Models：噪声建模与采样流程](#3.video-diffusion-models噪声建模与采样流程)
  - [面试问题：什么是 Video Diffusion Models？](#面试问题什么是-video-diffusion-models)
  - [面试问题：视频扩散模型的前向加噪过程是如何工作的？](#面试问题视频扩散模型的前向加噪过程是如何工作的)
  - [面试问题：视频扩散模型的反向去噪过程是如何实现的？](#面试问题视频扩散模型的反向去噪过程是如何实现的)
  - [面试问题：视频扩散模型中的噪声预测网络如何设计？](#面试问题视频扩散模型中的噪声预测网络如何设计)
  - [面试问题：视频扩散模型的训练目标通常是什么？](#面试问题视频扩散模型的训练目标通常是什么)
  - [面试问题：Classifier-Free Guidance 在视频生成中起什么作用？](#面试问题classifier-free-guidance-在视频生成中起什么作用)

[4.DiT、FiT、U-Net：扩散主干网络怎么选](#4.ditfitu-net扩散主干网络怎么选)
  - [面试问题：U-Net 为什么会成为早期扩散模型的主干？](#面试问题u-net-为什么会成为早期扩散模型的主干)
  - [面试问题：DiT 是什么？为什么视频生成逐渐转向 Transformer？](#面试问题dit-是什么为什么视频生成逐渐转向-transformer)
  - [面试问题：U-Net 和 DiT 的核心差异是什么？](#面试问题u-net-和-dit-的核心差异是什么)
  - [面试问题：FiT 在视频生成架构中应该怎么理解？](#面试问题fit-在视频生成架构中应该怎么理解)
  - [面试问题：AdaLN / FiLM 类调制为什么在 DiT 中很常见？](#面试问题adaln--film-类调制为什么在-dit-中很常见)

[5.视频扩散的特殊改造：时序建模、时空注意力、一致性](#5.视频扩散的特殊改造时序建模时空注意力一致性)
  - [面试问题：为什么不能直接把图像扩散模型简单扩展到视频？](#面试问题为什么不能直接把图像扩散模型简单扩展到视频)
  - [面试问题：视频扩散模型里常见的时序建模方法有哪些？](#面试问题视频扩散模型里常见的时序建模方法有哪些)
  - [面试问题：时空注意力和纯空间注意力有什么区别？](#面试问题时空注意力和纯空间注意力有什么区别)
  - [面试问题：什么是 3D RoPE？它在视频生成中有什么优势？](#面试问题什么是-3d-rope它在视频生成中有什么优势)
  - [面试问题：视频扩散模型中的时间一致性问题具体指什么？](#面试问题视频扩散模型中的时间一致性问题具体指什么)
  - [面试问题：视频生成为什么更容易出现 flicker 和 drift？](#面试问题视频生成为什么更容易出现-flicker-和-drift)

[6.视频自动标注模型与数据流水线](#6.视频自动标注模型与数据流水线)
  - [面试问题：什么是视频自动标注？它在视频生成中有什么作用？](#面试问题什么是视频自动标注它在视频生成中有什么作用)
  - [面试问题：视频自动标注和图像自动标注有什么区别？](#面试问题视频自动标注和图像自动标注有什么区别)
  - [面试问题：主流视频自动标注方法可以分为哪几类？](#面试问题主流视频自动标注方法可以分为哪几类)
  - [面试问题：CLIP、BLIP 系列模型如何用于视频自动标注？](#面试问题clipblip-系列模型如何用于视频自动标注)
  - [面试问题：视频专用多模态模型在自动标注中有什么价值？](#面试问题视频专用多模态模型在自动标注中有什么价值)
  - [面试问题：如何评估视频自动标注的质量？](#面试问题如何评估视频自动标注的质量)

[7.视频插帧模型：VFI 的核心方法与选型](#7.视频插帧模型vfi的核心方法与选型)
  - [面试问题：什么是视频插帧？它有哪些典型应用场景？](#面试问题什么是视频插帧它有哪些典型应用场景)
  - [面试问题：视频插帧的核心挑战是什么？](#面试问题视频插帧的核心挑战是什么)
  - [面试问题：基于光流的插帧方法基本原理是什么？](#面试问题基于光流的插帧方法基本原理是什么)
  - [面试问题：RIFE、DAIN、AMT 等插帧模型的主要差异是什么？](#面试问题rifedainamt-等插帧模型的主要差异是什么)
  - [面试问题：多尺度融合为什么对视频插帧很重要？](#面试问题多尺度融合为什么对视频插帧很重要)
  - [面试问题：视频插帧模型常用哪些评估指标？](#面试问题视频插帧模型常用哪些评估指标)

[8.AI 视频生成基础架构总览](#8.ai视频生成基础架构总览)
  - [面试问题：AI 视频生成系统的核心工作流程是什么？](#面试问题ai-视频生成系统的核心工作流程是什么)
  - [面试问题：AI 视频生成的核心组件通常包括哪些？](#面试问题ai-视频生成的核心组件通常包括哪些)
  - [面试问题：VAE 编码器和解码器在视频生成中分别负责什么？](#面试问题vae-编码器和解码器在视频生成中分别负责什么)
  - [面试问题：DiT / Transformer 主干网络如何处理时空信息？](#面试问题dit--transformer-主干网络如何处理时空信息)
  - [面试问题：噪声调度器在扩散模型中起什么作用？](#面试问题噪声调度器在扩散模型中起什么作用)
  - [面试问题：视频生成系统部署时常见的计算瓶颈有哪些？](#面试问题视频生成系统部署时常见的计算瓶颈有哪些)

---

<h1 id="1.文本与多模态条件编码器t5clip及条件注入">1.文本与多模态条件编码器：T5、CLIP 及条件注入</h1>

### 🔑 核心组件
| 编码器 | 类型 | 主要作用 | 特点 |
|--------|------|----------|------|
| T5 | Text Encoder | 文本语义理解 | 语言理解能力强，适合长文本提示词 |
| CLIP | Vision-Language Encoder | 图文语义对齐 | 跨模态对齐能力强，适合视觉语义约束 |

<h2 id="面试问题为什么-ai-视频模型里经常会出现-t5-和-clip">面试问题：为什么 AI 视频模型里经常会出现 T5 和 CLIP？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

AI 视频模型里经常出现 T5 和 CLIP，核心原因是：**视频生成模型本身主要负责生成时空内容，而 T5 和 CLIP 负责把用户输入的文本、图像或多模态条件转换成模型可理解的语义表示。**

从架构上看，视频生成模型通常不是直接读取自然语言 prompt，而是先通过条件编码器得到一组条件特征，再把这些特征注入到扩散模型或 Transformer 主干中。T5 和 CLIP 就是最常见的两类条件编码器。

T5 的优势在于**文本语义理解能力强**。它来自大规模语言建模任务，适合处理较长、较复杂的提示词，能够更好理解句子结构、属性关系、动作描述和组合语义。比如“一个穿红色雨衣的人在夜晚的街道上慢慢转身”这类 prompt，T5 更擅长捕捉其中的修饰关系和动作描述。

CLIP 的优势在于**视觉语义对齐能力强**。它通过图像和文本对比学习训练，文本 embedding 天然和视觉概念空间对齐，因此更适合提供“什么样的画面更符合这段文本”的视觉约束。在图像到视频、参考图驱动视频、风格控制等任务中，CLIP 往往能帮助模型保持更稳定的视觉语义。

所以面试里可以这样回答：**T5 偏语言理解，CLIP 偏视觉对齐。AI 视频模型使用它们，是为了把自然语言或多模态输入转成高质量条件信号，从而提升 prompt following、视觉语义一致性和生成可控性。**

<h2 id="面试问题t5-和-clip-的核心差异是什么">面试问题：T5 和 CLIP 的核心差异是什么？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

T5 和 CLIP 的核心差异在于：**T5 是语言模型视角的文本编码器，重点是理解文本本身；CLIP 是视觉 - 语言对齐模型，重点是让文本和图像落到可比较的语义空间里。**

可以从几个维度理解：

| 对比维度 | T5 | CLIP |
|----------|----|------|
| 训练目标 | 文本到文本的语言建模或去噪式文本理解 | 图像 - 文本对比学习 |
| 核心能力 | 长文本理解、语义组合、句法关系建模 | 图文语义对齐、视觉概念匹配 |
| 输出特征 | 更偏语言语义表示 | 更偏视觉语义对齐表示 |
| 适合场景 | 复杂 prompt、动作关系、属性组合 | 图文匹配、视觉风格、参考图语义约束 |

T5 更像是“**读懂用户到底说了什么**”。它擅长处理较长的自然语言描述，能保留更细粒度的文本语义，例如主体、动作、属性、场景之间的关系。

CLIP 更像是“**判断这段文字对应什么视觉概念**”。它的文本表示不是单纯为语言理解服务，而是为了和图像表示对齐，因此更适合给生成模型提供视觉语义方向。

需要注意的是，CLIP 并不等价于一个通用大语言模型。它对视觉概念、类别、风格、构图等很敏感，但对复杂逻辑、长文本推理、细粒度动作顺序的理解通常不如专门的语言编码器。

面试中可以总结为：**T5 强在语言理解，CLIP 强在图文对齐；前者帮助模型理解 prompt 的语义结构，后者帮助模型生成更符合视觉语义的内容。**

<h2 id="面试问题为什么有些模型要同时使用-t5-和-clip">面试问题：为什么有些模型要同时使用 T5 和 CLIP？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

**有些视频生成模型同时使用 T5 和 CLIP**，是因为它们提供的是**互补的条件信息**：T5 补强语言理解，CLIP 补强视觉对齐。对于视频生成这种高维、长时序、多约束任务，只依赖单一条件编码器往往不够稳定。

具体来说，T5 主要解决“prompt 语义是否被读懂”的问题。它能帮助模型理解更长的描述、更复杂的修饰关系和动作过程，例如主体是谁、发生了什么动作、动作顺序如何、场景和风格有什么限制。

CLIP 主要解决“生成画面是否符合视觉语义”的问题。它的表示空间经过图文对齐训练，更容易把文本概念和视觉外观联系起来，例如“cinematic lighting”“oil painting style”“a corgi wearing sunglasses”这类视觉概念。

在实际架构中，两者常见的融合方式包括：

1. **特征拼接或组合**：把 T5 和 CLIP 的文本特征一起作为条件输入。
2. **分别注入不同模块**：例如 T5 hidden states 用于 cross-attention，CLIP pooled embedding 用于全局调制。
3. **多路条件控制**：T5 控制细粒度语义，CLIP 控制视觉风格、主体语义或参考图对齐。

这样做的收益是生成结果通常会有更好的 prompt following 和视觉一致性。尤其在视频里，模型不仅要生成单帧画面，还要让主体、动作和场景在多帧中连续稳定，多源条件可以提供更强的约束。

当然，代价也很明显：同时使用多个编码器会增加显存、推理延迟和工程复杂度，还需要设计好不同条件之间的融合方式，避免语义冲突。

面试里可以回答：**同时使用 T5 和 CLIP，本质上是用 T5 负责“读懂语言”，用 CLIP 负责“对齐视觉”，二者互补可以提升视频生成的语义准确性、视觉一致性和可控性。**

<h2 id="面试问题条件信息通常是如何注入到视频生成模型里的">面试问题：条件信息通常是如何注入到视频生成模型里的？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

条件信息注入指的是：**把文本、图像、音频、动作、姿态、深度图、边缘图等外部控制信号，转化为生成模型内部可使用的特征，并影响去噪或生成过程。**

在 AI 视频生成中，常见的条件注入方式主要有以下几类：

| 注入方式 | 核心思路 | 典型用途 |
|----------|----------|----------|
| Cross-Attention | 让视频 latent 或 token 主动关注文本、多模态条件 token | 文本到视频、图像到视频、多模态生成 |
| 拼接输入 | 将条件图、mask、姿态图等与 noisy latent 在通道或 token 维度拼接 | 图像编辑、局部控制、结构控制 |
| AdaLN / FiLM 调制 | 用条件向量生成归一化层的缩放和平移参数 | DiT 类架构中的全局条件调制 |
| ControlNet / Adapter | 额外分支编码结构条件，再注入主干网络 | 姿态、深度、边缘、轨迹等精细控制 |
| 初始 latent 条件 | 用参考图或首帧编码后的 latent 初始化或约束生成过程 | Image-to-Video、首帧驱动视频生成 |

其中最常见的是 Cross-Attention。文本编码器先把 prompt 编成一组条件 token，视频生成主干中的 latent token 再通过 attention 去读取这些条件，从而知道每个时空位置应该生成什么内容。

对于 DiT 或 Transformer 类视频生成模型，条件也常通过 AdaLN、FiLM 或类似调制方式注入。简单理解，就是条件向量不直接作为 token 参与 attention，而是改变网络中间层的归一化、缩放或偏移参数，从而影响整体生成方向。

对于可控视频生成，条件往往更复杂。例如姿态控制会输入人体骨架，结构控制会输入 depth、canny、segmentation，视频编辑会输入 mask 或参考帧。这类条件通常需要额外编码器或 ControlNet 类分支，避免模型只知道“生成什么”，却不知道“按照什么结构生成”。

面试中可以总结为：**条件注入的本质是把外部控制信号变成生成网络内部的约束。文本语义常用 cross-attention，整体风格或类别可用调制，结构控制常用拼接、Adapter 或 ControlNet。**

<h2 id="面试问题cross-attention-为什么在视频生成里很重要">面试问题：Cross-Attention 为什么在视频生成里很重要？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

Cross-Attention 在视频生成里很重要，核心原因是：**它提供了一种灵活的条件读取机制，让视频 latent 或时空 token 能够在生成过程中动态关注文本或多模态条件。**

在文本到视频任务中，模型内部处理的是 noisy video latent 或时空 token，而用户输入的是自然语言 prompt。两者模态不同，不能简单相加。Cross-Attention 的作用就是建立二者之间的桥梁：视频 token 作为 query，文本 token 作为 key 和 value，让每个时空位置根据需要读取相关的文本语义。

这带来几个关键好处：

1. **提升 prompt following 能力**：模型可以根据不同 token 关注不同文本片段，例如主体、动作、场景、风格。
2. **支持细粒度语义控制**：相比只用一个全局文本向量，cross-attention 可以保留 token 级语义，更适合复杂 prompt。
3. **适合时空生成任务**：视频中不同帧、不同区域可能对应不同动作和内容，cross-attention 能让每个时空位置动态读取条件。
4. **便于扩展多模态条件**：除了文本，也可以接入图像 token、音频 token、参考帧特征或其他控制信号。

举例来说，如果 prompt 是“a dog running on the beach at sunset”，模型在生成主体区域时需要关注 “dog”，在生成动作时需要关注 “running”，在生成背景和光照时需要关注 “beach” 和 “sunset”。Cross-Attention 允许这些语义在生成过程中被不同位置分别读取，而不是被压缩成一个粗糙的全局条件。

在视频生成中，Cross-Attention 还和时序一致性有关。如果条件注入不稳定，不同帧可能对 prompt 的关注不一致，容易导致主体变化、动作不连贯或风格漂移。因此很多视频模型会结合时空 attention、条件调制和一致性约束来增强跨帧稳定性。

面试里可以回答：**Cross-Attention 是文本或多模态条件和视频生成主干之间的核心连接方式，它让视频 token 能够按需读取条件语义，从而提升 prompt following、细粒度控制和跨帧语义一致性。**

---

<h1 id="2.vaeldm与潜空间建模">2.VAE、LDM 与潜空间建模</h1>

### 🔑 关键概念
- **VAE（Variational Autoencoder）**：将像素空间视频压缩到更低维的潜空间。
- **LDM（Latent Diffusion Model）**：在潜空间中执行扩散和去噪，降低计算成本。
- **KL Loss**：约束潜变量分布，使潜空间更连续、更可采样。

<h2 id="面试问题什么是-vae它在-ai-视频里有什么作用">面试问题：什么是 VAE？它在 AI 视频里有什么作用？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

VAE（Variational Autoencoder，变分自编码器）是一类**带概率约束的自编码模型**，由编码器、潜变量分布和解码器组成。它的核心思想是：编码器不直接输出一个确定的 latent，而是输出潜变量分布的参数；模型再从这个分布中采样 latent，并由解码器重建原始数据。

对于图像或视频样本 $x$，VAE 通常建模为：

$$
q_\phi(z \mid x) = \mathcal{N}(\mu_\phi(x), \sigma_\phi^2(x) I)
$$

其中 $q_\phi(z \mid x)$ 是编码器给出的近似后验分布， $\mu_\phi(x)$ 和 $\sigma_\phi(x)$ 分别表示均值和标准差。采样时通常使用重参数化技巧：

$$
z = \mu_\phi(x) + \sigma_\phi(x) \odot \epsilon, \qquad \epsilon \sim \mathcal{N}(0, I)
$$

这样可以在保留随机采样的同时，让梯度能够从解码器反传到编码器。

VAE 的训练目标通常来自 ELBO（Evidence Lower Bound），可以写成：

$$
\mathcal{L}_{VAE}
= \mathbb{E}_{q_\phi(z \mid x)}[-\log p_\theta(x \mid z)] + \beta D_{KL}(q_\phi(z \mid x) \Vert p(z))
$$

其中第一项是重建损失，要求解码器能从 latent 还原原始图像或视频；第二项是 KL 正则项，要求编码后的潜变量分布接近先验分布 $p(z)$，通常取标准高斯分布 $\mathcal{N}(0, I)$。

在 AI 视频生成里，VAE 的作用非常关键：**它负责把高维像素空间的视频压缩到低维潜空间，并在生成结束后把潜变量解码回视频帧。**

典型流程可以概括为：

```text
原始视频 x -> VAE Encoder -> 视频 latent z
生成模型在 z 空间中建模或去噪
生成后的 latent -> VAE Decoder -> 输出视频
```

在 Stable Diffusion、Latent Diffusion Models、Stable Video Diffusion、AnimateDiff、VideoCrafter 等模型体系中，VAE 或类似 autoencoder 都承担了“像素空间和潜空间之间的桥梁”角色。

面试里可以回答：**VAE 是一种概率自编码器，在 AI 视频里主要用于压缩和重建视频。它把像素级视频映射到更紧凑的 latent space，让后续扩散模型不必直接在巨大像素空间中建模，从而显著降低计算成本。**

<h2 id="面试问题为什么视频扩散模型通常要在潜空间建模">面试问题：为什么视频扩散模型通常要在潜空间建模？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

视频扩散模型通常在潜空间建模，核心原因是：**直接在像素空间对视频做扩散代价太高，而潜空间可以大幅降低时空维度上的计算量，同时保留主要语义和结构信息。**

视频数据的维度非常高。一个视频样本通常可以表示为：

$$
x \in \mathbb{R}^{T \times C \times H \times W}
$$

其中 $T$ 是帧数， $C$ 是通道数， $H$ 和 $W$ 是空间分辨率。如果直接在像素空间做扩散，噪声预测网络需要同时处理大量时空像素，显存、算力和训练成本都会非常高。

LDM（Latent Diffusion Model）的核心思想是：**先用 autoencoder 或 VAE 把数据压缩到 latent space，再在 latent space 中做扩散建模。**

可以写成：

$$
z = E(x), \qquad \hat{x} = D(z)
$$

扩散过程不再作用于原始像素 $x$，而是作用于潜变量 $z$：

$$
z_t = \sqrt{\bar{\alpha}_t} z_0 + \sqrt{1 - \bar{\alpha}_t} \epsilon, \qquad \epsilon \sim \mathcal{N}(0, I)
$$

训练时，噪声预测网络学习从带噪 latent 中预测噪声：

$$
\mathcal{L}_{LDM}
= \mathbb{E}_{z_0, t, \epsilon}
\left[
\left\| \epsilon - \epsilon_\theta(z_t, t, c) \right\|_2^2
\right]
$$

其中 $c$ 表示文本、图像或其他条件信息， $\epsilon_\theta$ 是扩散模型的噪声预测网络。

潜空间建模的优势主要有三点：

1. **显著降低计算成本**：空间分辨率通常被压缩到原来的 $1/4$、 $1/8$ 甚至更低，视频任务中节省尤其明显。
2. **保留更高层语义**：latent 通常包含主体、结构、纹理和运动相关信息，比纯像素更适合生成模型学习。
3. **提升工程可扩展性**：高分辨率、长视频、多条件控制都更容易在 latent space 中实现。

典型模型包括 Latent Diffusion Models（LDM）、Stable Diffusion、Stable Video Diffusion、VideoCrafter、ModelScope Text-to-Video 等。它们的共同思想都是：**将生成难题从像素空间转移到更紧凑的潜空间。**

面试中可以总结为：**视频扩散模型使用潜空间建模，是为了在保证语义表达能力的同时降低时空计算量。VAE 负责压缩和解码，扩散模型负责在 latent space 中学习生成分布，这是现代高效视频生成架构的基础。**

<h2 id="面试问题vae-的-kl-loss-是干什么的">面试问题：VAE 的 KL Loss 是干什么的？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐ (3/5)**

VAE 的 KL Loss 用来约束编码器输出的潜变量分布，使其接近预设先验分布，通常是标准高斯分布。它的核心作用是：**让 latent space 连续、规整、可采样，而不是变成一堆离散且无结构的编码点。**

在 VAE 中，编码器输出的是近似后验分布：

$$
q_\phi(z \mid x) = \mathcal{N}(\mu, \sigma^2 I)
$$

先验分布通常设为：

$$
p(z) = \mathcal{N}(0, I)
$$

KL Loss 衡量二者之间的分布差异：

$$
D_{KL}(q_\phi(z \mid x) \Vert p(z))
$$

对于对角高斯分布，KL 项可以写成常见的闭式形式：

$$
D_{KL}
= \frac{1}{2} \sum_i
\left(
\mu_i^2 + \sigma_i^2 - \log \sigma_i^2 - 1
\right)
$$

如果没有 KL Loss，编码器可能会把不同样本映射到非常分散、不可控的 latent 区域，解码器虽然能重建训练样本，但 latent space 不连续、不平滑，生成模型很难在其中学习稳定分布。

KL Loss 的作用可以从三个角度理解：

1. **约束潜空间分布**：让编码结果不要偏离标准高斯太远。
2. **增强可采样性**：从先验分布采样 latent 时，解码器更可能生成合理样本。
3. **提升连续性和平滑性**：相近 latent 对应相近内容，便于扩散模型在潜空间中去噪。

但 KL Loss 也不能过强。如果 KL 权重太大，模型会过度压缩信息，导致重建模糊、细节丢失，甚至出现 posterior collapse。实际工程里通常需要在重建质量和潜空间规整性之间做权衡。

面试里可以回答：**KL Loss 的本质是让 VAE 的编码分布接近标准高斯先验，使 latent space 更连续、更平滑、更适合采样和扩散建模；但 KL 约束过强会损伤重建质量。**

<h2 id="面试问题为什么说-vae-会带来信息损失">面试问题：为什么说 VAE 会带来信息损失？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

说 VAE 会带来信息损失，主要是因为它不是简单地无损压缩，而是把高维像素数据压缩到更低维、更规整的潜空间中。这个过程必然会丢掉一部分难以压缩或被模型认为不重要的细节。

从编码 - 解码过程看：

$$
z = E(x), \qquad \hat{x} = D(z)
$$

理想情况下希望 $\hat{x} \approx x$，但由于 latent 维度更低、编码器容量有限、KL 正则存在，重建结果通常不可能完全等于原始输入。

VAE 的信息损失主要来自几个方面：

1. **空间压缩损失**：VAE 通常会下采样图像或视频，把高分辨率像素压缩成低分辨率 latent，细纹理、文字、小物体容易丢失。
2. **通道瓶颈损失**：latent 的通道数和表达容量有限，无法完整保留所有像素级信息。
3. **KL 正则损失**：为了让 latent 分布接近标准高斯，模型会牺牲一部分重建精度来换取潜空间规整性。
4. **解码器重建误差**：解码器本身可能无法完美恢复高频纹理、锐利边缘和复杂运动细节。

在图像生成中，VAE 信息损失常表现为：小字不清晰、纹理发糊、边缘不够锐利。
在视频生成中，还会进一步表现为：细节闪烁、局部纹理不稳定、快速运动区域模糊、跨帧细节不一致。

这也是为什么现代生成模型中 VAE 的设计非常关键。比如 Stable Diffusion 系列依赖高质量图像 VAE，Stable Video Diffusion、VideoCrafter 等视频模型则需要更关注时间维度上的重建一致性。如果 VAE 本身重建能力不足，后面的扩散主干再强，也很难恢复已经丢失的细节。

面试中可以总结为：**VAE 的信息损失来自压缩瓶颈、KL 正则和解码误差。它用一定的细节损失换取低维、连续、可建模的 latent space；在视频任务中，这种损失不仅影响单帧清晰度，还会影响跨帧一致性。**

<h2 id="面试问题视频-vae-和图像-vae-有什么不同">面试问题：视频 VAE 和图像 VAE 有什么不同？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

视频 VAE 和图像 VAE 的核心不同在于：**图像 VAE 只需要压缩和重建单帧空间内容，而视频 VAE 还必须处理时间维度，保证多帧重建的一致性和运动连续性。**

图像 VAE 的输入通常是：

$$
x \in \mathbb{R}^{C \times H \times W}
$$

视频 VAE 的输入通常是：

$$
x \in \mathbb{R}^{T \times C \times H \times W}
$$

因此视频 VAE 不仅要压缩空间维度 $H, W$，还要决定如何处理时间维度 $T$。不同模型会有不同设计：

| 类型 | 核心思路 | 特点 |
|------|----------|------|
| 逐帧图像 VAE | 对每一帧独立编码和解码 | 简单、可复用图像 VAE，但时序一致性弱 |
| 2D + Temporal 模块 | 空间用 2D CNN，时间维度加时序卷积或 attention | 在效率和时序建模之间折中 |
| 3D VAE | 使用 3D 卷积同时压缩时间和空间 | 更适合视频重建，但计算成本更高 |
| Causal Video VAE | 只依赖当前帧及历史帧进行编码或解码 | 适合流式生成和长视频建模 |

视频 VAE 的关键挑战包括：

1. **时序一致性**：相邻帧重建后不能出现颜色、纹理、边缘的随机抖动。
2. **运动保真**：快速运动、遮挡、形变区域不能被过度平滑。
3. **压缩率选择**：空间压缩可以降低成本，但时间压缩如果过强，容易丢失动作细节。
4. **长视频扩展**：视频越长，VAE 的显存和时序稳定性压力越大。

典型模型中，早期很多视频生成方法直接复用图像 VAE，对每帧独立编码，这种方式实现简单，但容易出现 flicker。更强的视频生成系统会使用视频专用 autoencoder，例如包含 3D 卷积、temporal attention 或 causal temporal compression 的 Video VAE，以更好保留跨帧结构。

如果从 latent 形状看，图像 latent 可能是：

$$
z \in \mathbb{R}^{C_z \times H' \times W'}
$$

而视频 latent 通常是：

$$
z \in \mathbb{R}^{T' \times C_z \times H' \times W'}
$$

其中 $T'$ 可能等于 $T$，也可能经过时间下采样。时间维度是否压缩，是视频 VAE 设计中的重要选择。

面试里可以总结为：**图像 VAE 解决的是空间压缩和重建，视频 VAE 还要解决时间维度上的压缩、运动保真和跨帧一致性。视频 VAE 设计得不好，会直接导致生成视频模糊、闪烁和时序不稳定。**

---

<h1 id="3.video-diffusion-models噪声建模与采样流程">3.Video Diffusion Models：噪声建模与采样流程</h1>

### 🔑 扩散过程
```text
前向过程（加噪）：x0 -> x1 -> ... -> xt
反向过程（去噪）：xt -> xt-1 -> ... -> x0
```

<h2 id="面试问题什么是-video-diffusion-models">面试问题：什么是 Video Diffusion Models？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

Video Diffusion Models 指的是：**把扩散模型应用到视频生成任务中，通过逐步加噪和逐步去噪来学习视频数据分布的生成模型。**

它的基本思想和图像扩散模型一致：训练时把干净视频逐步加噪，直到接近高斯噪声；生成时从随机噪声出发，通过神经网络逐步去噪，最终得到一段符合条件的视频。

如果视频在像素空间中表示为：

$$
x_0 \in \mathbb{R}^{T \times C \times H \times W}
$$

在潜空间视频扩散中，更常见的是先用 VAE 编码成：

$$
z_0 = E(x_0), \qquad z_0 \in \mathbb{R}^{T' \times C_z \times H' \times W'}
$$

然后扩散模型在 $z_0$ 上执行加噪和去噪。现代视频生成模型通常采用这种 latent video diffusion 形式，而不是直接在原始像素空间中扩散。

Video Diffusion Models 的核心组成通常包括：

| 组件 | 作用 |
|------|------|
| VAE / Video VAE | 将视频压缩到 latent space，并在最后解码回视频 |
| 噪声调度器 | 定义每一步加噪和去噪的强度 |
| 噪声预测网络 | 预测当前 noisy latent 中的噪声或其他目标 |
| 条件编码器 | 编码文本、图像、音频、首帧、动作等条件 |
| 采样器 | 按 DDPM、DDIM、DPM-Solver 等策略执行反向去噪 |

与图像扩散相比，视频扩散最大的不同在于输入多了时间维度。模型不仅要生成单帧画面，还要保证帧间运动连续、主体身份稳定、场景结构一致。因此视频扩散模型通常需要引入 3D U-Net、Temporal Attention、Space-Time Attention、DiT / Video Transformer、3D RoPE 等时空建模机制。

典型模型包括 Video Diffusion Models、Imagen Video、Make-A-Video、ModelScope Text-to-Video、VideoCrafter、Stable Video Diffusion、AnimateDiff、Sora 类视频生成模型等。它们的共同点是：**用扩散过程建模视频分布，用时空网络处理视频的时间和空间结构。**

面试里可以回答：**Video Diffusion Models 是用扩散生成框架来生成视频的模型。它通过前向加噪学习视频数据的噪声扰动过程，再通过反向去噪从随机噪声恢复视频；相比图像扩散，关键难点在于时序建模、运动一致性和长程依赖。**

<h2 id="面试问题视频扩散模型的前向加噪过程是如何工作的">面试问题：视频扩散模型的前向加噪过程是如何工作的？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

视频扩散模型的前向加噪过程，本质上是一个固定的马尔可夫过程：**从干净视频 latent 开始，在每个时间步逐渐加入高斯噪声，最后把数据变成接近标准高斯噪声的形式。**

假设干净视频 latent 为 $z_0$，前向扩散过程可以写成：

$$
q(z_t \mid z_{t-1})
= \mathcal{N}(\sqrt{1-\beta_t} z_{t-1}, \beta_t I)
$$

其中 $\beta_t$ 是第 $t$ 步的噪声强度，通常由 noise schedule 预先设定。定义：

$$
\alpha_t = 1 - \beta_t, \qquad \bar{\alpha}_t = \prod_{s=1}^{t} \alpha_s
$$

则可以直接从 $z_0$ 采样任意时间步的带噪 latent：

$$
q(z_t \mid z_0)
= \mathcal{N}(\sqrt{\bar{\alpha}_t} z_0, (1-\bar{\alpha}_t) I)
$$

等价地，可以写成常见的闭式加噪公式：

$$
z_t = \sqrt{\bar{\alpha}_t} z_0 + \sqrt{1-\bar{\alpha}_t} \epsilon,
\qquad \epsilon \sim \mathcal{N}(0, I)
$$

这个公式非常重要，面试中经常会问。它说明训练时不需要一步步真的从 $z_0$ 加噪到 $z_t$，而是可以随机采样一个时间步 $t$，直接构造对应强度的 noisy latent。

在视频扩散中， $z_t$ 仍然保留视频的时空结构，例如：

$$
z_t \in \mathbb{R}^{T' \times C_z \times H' \times W'}
$$

噪声通常会加到整个视频 latent 上，也就是每一帧、每个空间位置、每个通道都会被扰动。随着 $t$ 变大，视频中的结构、纹理和运动信息逐渐被噪声淹没；当 $t$ 接近最大时间步时， $z_t$ 基本接近高斯噪声。

前向过程本身不需要学习，它是人为设计好的固定过程。模型真正要学习的是反向过程，即如何从 $z_t$ 一步步恢复到 $z_0$。

面试里可以总结为：**前向加噪就是按照预设噪声调度，把干净视频 latent 逐步扰动成高斯噪声。** 关键公式是 $z_t = \sqrt{\bar{\alpha}_t} z_0 + \sqrt{1-\bar{\alpha}_t}\epsilon$，它允许训练时直接构造任意噪声等级的样本。

<h2 id="面试问题视频扩散模型的反向去噪过程是如何实现的">面试问题：视频扩散模型的反向去噪过程是如何实现的？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

视频扩散模型的反向去噪过程，是指：**从一个随机高斯噪声视频 latent 出发，通过神经网络逐步预测并去除噪声，最终恢复出清晰的视频 latent，再由 VAE 解码成视频。**

理论上，反向过程可以写成：

$$
p_\theta(z_{t-1} \mid z_t, c)
= \mathcal{N}(\mu_\theta(z_t, t, c), \Sigma_\theta(z_t, t, c))
$$

其中 $c$ 是条件信息，例如文本 prompt、参考图、首帧、姿态或音频； $\mu_\theta$ 和 $\Sigma_\theta$ 是模型预测的反向分布参数。

实际工程中，最常见的做法不是直接预测 $z_{t-1}$，而是训练网络预测噪声 $\epsilon$：

$$
\epsilon_\theta(z_t, t, c) \approx \epsilon
$$

有了预测噪声后，可以先估计干净 latent：

$$
\hat{z}_0
= \frac{z_t - \sqrt{1-\bar{\alpha}_t}\epsilon_\theta(z_t, t, c)}
{\sqrt{\bar{\alpha}_t}}
$$

再根据采样器规则从 $z_t$ 更新到 $z_{t-1}$。DDPM 的反向均值常写成：

$$
\mu_\theta(z_t, t, c)
= \frac{1}{\sqrt{\alpha_t}}
\left(
z_t - \frac{\beta_t}{\sqrt{1-\bar{\alpha}_t}}
\epsilon_\theta(z_t, t, c)
\right)
$$

然后进行采样：

$$
z_{t-1} = \mu_\theta(z_t, t, c) + \sigma_t \eta,
\qquad \eta \sim \mathcal{N}(0, I)
$$

其中 $\sigma_t$ 控制采样随机性。如果使用 DDIM 或一些 ODE/Solver 类采样器，反向过程可以更接近确定性，并用更少步数完成生成。

视频扩散的反向去噪比图像更难，因为每一步去噪都必须同时满足三类要求：

1. **单帧质量**：每一帧要清晰、自然、结构合理。
2. **时序一致性**：相邻帧之间不能闪烁、跳变或身份漂移。
3. **条件对齐**：整体视频要符合文本、首帧、动作或其他控制条件。

因此，反向去噪网络通常需要时空联合建模能力，例如 3D U-Net、Temporal Attention、Space-Time Transformer 或 DiT 类架构。仅靠逐帧去噪，很容易得到“单帧还行，但播放起来不连贯”的视频。

面试中可以回答：**反向去噪就是从随机视频噪声 latent 开始，利用噪声预测网络逐步估计并去除噪声，最终得到干净视频 latent。** 核心是学习 $p_\theta(z_{t-1} \mid z_t, c)$，工程上通常通过预测噪声 $\epsilon_\theta(z_t,t,c)$ 来实现。

<h2 id="面试问题视频扩散模型中的噪声预测网络如何设计">面试问题：视频扩散模型中的噪声预测网络如何设计？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

视频扩散模型中的噪声预测网络，是整个模型的生成主干。它的任务是：**输入带噪视频 latent、扩散时间步和条件信息，输出当前噪声、干净 latent 或 velocity 等预测目标。**

最常见的形式是噪声预测：

$$
\epsilon_\theta(z_t, t, c)
$$

其中 $z_t$ 是带噪视频 latent， $t$ 是扩散时间步， $c$ 是条件信息。网络必须根据当前噪声等级和条件信息，判断哪些部分是噪声、哪些部分应该被保留或恢复。

视频噪声预测网络通常要解决两个维度的问题：

| 维度 | 需要建模的内容 |
|------|----------------|
| 空间维度 | 单帧中的物体、纹理、背景、构图和细节 |
| 时间维度 | 帧间运动、身份一致性、动作连续性和长程依赖 |

常见设计路线包括：

| 架构类型 | 核心思想 | 代表特点 |
|----------|----------|----------|
| 3D U-Net | 用 3D 卷积同时处理时间和空间 | 局部时空建模强，结构接近图像扩散 U-Net |
| 2D U-Net + Temporal Module | 复用图像 U-Net，在中间层插入时间卷积或 temporal attention | 便于从图像模型迁移到视频模型 |
| Video Transformer / DiT | 把视频 latent 切成时空 token，用 Transformer 建模 | 全局依赖建模强，适合大规模视频生成 |
| Factorized Attention | 分别做空间 attention 和时间 attention | 降低计算成本，常用于长视频或高分辨率视频 |

噪声预测网络通常还需要注入三类信息：

1. **时间步信息**：通过 timestep embedding 告诉模型当前噪声强度。
2. **条件信息**：通过 cross-attention、AdaLN、FiLM、拼接或 ControlNet 等方式注入文本、图像、姿态等条件。
3. **位置和时序信息**：通过 2D/3D positional embedding、RoPE、3D RoPE 等方式编码帧位置和空间位置。

在视频任务中，一个关键设计原则是：**不能只把每帧当作独立图像去噪，而要让网络显式或隐式地看到多帧之间的关系。** 否则生成结果容易出现 flicker、身份漂移、动作不连贯等问题。

典型模型中，早期视频扩散常使用 3D U-Net 或在图像 U-Net 中插入 temporal attention；AnimateDiff 的核心思路就是给已有图像扩散模型添加 motion module；Stable Video Diffusion、VideoCrafter 等模型也会围绕时空注意力、视频 latent 和条件注入设计噪声预测主干；更大规模模型则逐渐转向 DiT / Transformer 风格。

面试里可以总结为：**视频扩散的噪声预测网络不仅要预测噪声，还要同时建模空间细节和时间动态。主流设计包括 3D U-Net、2D U-Net 加时序模块、Video Transformer / DiT，以及分解式时空注意力。**

<h2 id="面试问题视频扩散模型的训练目标通常是什么">面试问题：视频扩散模型的训练目标通常是什么？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

视频扩散模型的训练目标通常是：**随机选择一个扩散时间步，把干净视频 latent 加噪到该时间步，然后训练网络预测被加入的噪声或等价目标。**

最经典的是 DDPM 的噪声预测目标：

$$
\mathcal{L}_{simple}
= \mathbb{E}_{z_0, t, \epsilon, c}
\left[
\left\| \epsilon - \epsilon_\theta(z_t, t, c) \right\|_2^2
\right]
$$

其中：

- $z_0$ 是干净视频 latent
- $t$ 是随机采样的扩散时间步
- $\epsilon \sim \mathcal{N}(0, I)$ 是真实加入的高斯噪声
- $c$ 是条件信息
- $\epsilon_\theta$ 是噪声预测网络

训练流程可以概括为：

```text
采样视频 x0 -> VAE 编码得到 z0
随机采样时间步 t 和噪声 epsilon
构造 zt = sqrt(alpha_bar_t) z0 + sqrt(1-alpha_bar_t) epsilon
输入 zt、t、条件 c
训练网络预测 epsilon
```

除了预测噪声 $\epsilon$，现代扩散模型也常见其他预测参数化方式：

| 预测目标 | 含义 | 特点 |
|----------|------|------|
| $\epsilon$ prediction | 预测加入的噪声 | DDPM 中最经典，稳定易用 |
| $x_0$ / $z_0$ prediction | 直接预测干净样本或干净 latent | 直观，但不同噪声等级下尺度处理更敏感 |
| $v$ prediction | 预测 velocity 形式的目标 | 在一些模型中训练和采样更稳定 |

常见的 $v$ prediction 可以写成：

$$
v = \sqrt{\bar{\alpha}_t}\epsilon - \sqrt{1-\bar{\alpha}_t} z_0
$$

对应训练目标为：

$$
\mathcal{L}_{v}
= \mathbb{E}
\left[
\left\| v - v_\theta(z_t, t, c) \right\|_2^2
\right]
$$

视频扩散模型除了基本去噪损失，有时还会加入额外约束，例如：

1. **时序一致性损失**：约束相邻帧或特征在时间上稳定。
2. **重建或感知损失**：提升视觉质量和语义保真。
3. **运动约束**：利用光流、轨迹或姿态增强运动合理性。
4. **条件对齐损失**：提升文本、图像或动作条件的跟随能力。

但在大多数基础扩散模型中，核心训练目标仍然是去噪预测损失。其他损失更多是针对特定任务或工程需求的增强项。

面试中可以回答：**视频扩散模型最核心的训练目标是 denoising score matching，工程上通常表现为预测加入到视频 latent 中的噪声。** 具体来说，就是最小化 $\|\epsilon - \epsilon_\theta(z_t,t,c)\|_2^2$。也可以采用 $z_0$ prediction 或 $v$ prediction，并根据任务加入时序一致性或条件对齐约束。

<h2 id="面试问题classifier-free-guidance-在视频生成中起什么作用">面试问题：Classifier-Free Guidance 在视频生成中起什么作用？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐ (3/5)**

Classifier-Free Guidance（CFG）是一种在不额外训练分类器的情况下增强条件控制的方法。它的核心作用是：**在采样阶段放大条件信息的影响，让生成视频更符合文本、图像或其他控制条件。**

CFG 的训练方式很简单：训练时以一定概率丢弃条件，让同一个模型同时学会条件生成和无条件生成。也就是说，模型既能预测：

$$
\epsilon_\theta(z_t, t, c)
$$

也能预测：

$$
\epsilon_\theta(z_t, t, \varnothing)
$$

其中 $\varnothing$ 表示空条件或无条件输入。

采样时，将有条件预测和无条件预测组合起来：

$$
\hat{\epsilon}_\theta
= \epsilon_\theta(z_t, t, \varnothing) + s \left(
\epsilon_\theta(z_t, t, c) - \epsilon_\theta(z_t, t, \varnothing)
\right)
$$

其中 $s$ 是 guidance scale。也可以写成等价形式：

$$
\hat{\epsilon}_\theta
= (1+s)\epsilon_\theta(z_t, t, c) - s\epsilon_\theta(z_t, t, \varnothing)
$$

CFG 的直观理解是：无条件预测表示“模型自然会生成什么”，条件预测表示“在 prompt 约束下应该生成什么”。两者之差可以看作条件方向，guidance scale 就是在采样时放大这个方向。

在视频生成中，CFG 主要有几个作用：

1. **增强 prompt following**：让生成结果更贴近文本描述或多模态条件。
2. **提高主体和风格一致性**：减少模型偏离条件生成无关内容的概率。
3. **增强可控性**：用户可以通过调节 guidance scale 控制条件强度。

但 CFG 不是越大越好。guidance scale 过小，视频可能不听 prompt；guidance scale 过大，可能导致过饱和、细节伪影、运动僵硬、画面不自然，甚至破坏时序一致性。视频任务中这个问题更明显，因为过强条件约束可能让每帧都强行贴合 prompt，却牺牲自然运动和跨帧连续性。

典型实践中，文本到图像和文本到视频模型都会使用 CFG。Stable Diffusion、Imagen、VideoCrafter、Stable Video Diffusion 等扩散体系中，CFG 都是提升条件生成质量的重要采样技巧。

面试里可以总结为：**Classifier-Free Guidance 是采样阶段增强条件控制的技术。它通过组合有条件和无条件噪声预测，放大条件方向，从而提升 prompt following 和可控性；但 guidance scale 过大可能带来伪影、过拟合条件和视频时序不稳定。**

---

<h1 id="4.ditfitu-net扩散主干网络怎么选">4.DiT、FiT、U-Net：扩散主干网络怎么选</h1>

### 🔑 主干网络对比
| 架构 | 全称 | 核心特点 | 常见场景 |
|------|------|----------|----------|
| U-Net | U-Net | 多尺度卷积、跳跃连接 | 早期图像/视频扩散模型 |
| DiT | Diffusion Transformer | Token 化建模、全局注意力 | 大规模高质量生成 |
| FiT | Full-image Transformer | 更强调全图/全帧 Transformer 表示 | 高分辨率生成与统一建模 |

<h2 id="面试问题u-net-为什么会成为早期扩散模型的主干">面试问题：U-Net 为什么会成为早期扩散模型的主干？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

U-Net 会成为早期扩散模型的主干，核心原因是：**扩散模型本质上是一个逐步去噪任务，而 U-Net 的多尺度编码 - 解码结构非常适合做图像或视频 latent 的噪声预测。**

U-Net 最初用于医学图像分割，其结构特点是：

```text
输入 -> 下采样编码器 -> bottleneck -> 上采样解码器 -> 输出
             |                         ^
             |------ skip connection ---|
```

对于扩散模型来说，输入是带噪图像或带噪 latent，输出通常是噪声预测：

$$
\epsilon_\theta(x_t, t, c)
$$

这里 $x_t$ 或 $z_t$ 包含不同尺度的噪声和结构信息。U-Net 的优势正好对应这个任务需求：

1. **多尺度建模能力强**：下采样路径捕捉全局结构，上采样路径恢复局部细节。
2. **跳跃连接保留细节**：skip connection 能把浅层高分辨率特征直接传到解码端，适合恢复边缘、纹理和空间结构。
3. **卷积归纳偏置强**：卷积天然适合处理局部空间相关性，对图像生成任务非常有效。
4. **计算效率较高**：相比早期全局 Transformer，卷积 U-Net 在中低分辨率 latent 上更容易训练和部署。
5. **便于条件注入**：可以在不同层加入 timestep embedding、cross-attention、ControlNet、Adapter 等条件模块。

在 Stable Diffusion 这类 Latent Diffusion Model 中，U-Net 是噪声预测网络的核心。输入 noisy latent、扩散时间步和文本条件，输出预测噪声。视频扩散的早期方法也常在图像 U-Net 的基础上加入 temporal convolution、temporal attention 或 motion module，把 2D U-Net 扩展到视频场景。

不过，**U-Net 的局限也很明显**：它更依赖局部卷积和多尺度特征融合，对于长程依赖、全局 token 交互和大规模视频时空建模不如 Transformer 灵活。

因此在更大规模、更长视频、更高分辨率的模型中，主干逐渐向 DiT / Video Transformer 演进。

面试里可以回答：**U-Net 成为早期扩散主干，是因为它的多尺度编码 - 解码结构和扩散去噪任务高度匹配，既能保留局部细节，又能建模全局结构；但在大规模视频生成中，U-Net 的长程时空建模能力逐渐成为瓶颈。**

<h2 id="面试问题dit-是什么为什么视频生成逐渐转向-transformer">面试问题：DiT 是什么？为什么视频生成逐渐转向 Transformer？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

DiT（Diffusion Transformer）指的是：**用 Transformer 作为扩散模型噪声预测主干的架构范式。** 它把图像或视频 latent 切成 token，然后通过 Transformer block 进行全局建模，最后预测噪声、干净样本或 velocity。

以 latent diffusion 为例，假设视频 latent 为：

$$
z_t \in \mathbb{R}^{T' \times C_z \times H' \times W'}
$$

DiT / Video DiT 通常会把它切成时空 patch token：

$$
z_t \rightarrow \{p_1, p_2, \dots, p_N\}
$$

其中每个 token 表示一个空间 patch 或时空 patch。之后通过 Transformer 进行建模：

$$
h^{l+1} = \mathrm{TransformerBlock}(h^l, t, c)
$$

其中 $t$ 是扩散时间步， $c$ 是文本、图像或其他条件信息。

视频生成逐渐转向 Transformer，主要有几个原因：

1. **全局依赖建模更强**：Self-Attention 可以让远距离 token 直接交互，更适合长视频、复杂动作和全局一致性。
2. **更适合大规模训练**：Transformer 在语言模型和视觉模型中已经证明了很好的 scaling behavior，参数量、数据量和算力增加时性能通常更可预测。
3. **统一多模态建模更自然**：文本 token、图像 token、视频 token、音频 token 都可以在 token space 中统一处理。
4. **适合时空联合建模**：视频可以被表示成 space-time tokens，Transformer 可以同时建模空间结构和时间动态。
5. **架构更简洁统一**：相比卷积 U-Net 中复杂的多尺度设计，DiT 更接近通用 token 建模框架。

典型图像生成模型中，DiT 代表了从 U-Net 向 Transformer 扩散主干的转变；在视频生成中，Sora 类模型、许多 Video DiT / Diffusion Transformer 系统也体现了类似趋势：把视频压缩到 latent 后，用大规模 Transformer 在时空 token 上建模。

但 Transformer 也不是没有代价。Self-Attention 的计算复杂度通常随 token 数量平方增长：

$$
\mathcal{O}(N^2)
$$

视频 token 数量远大于图像，因为多了时间维度，所以长视频和高分辨率视频会带来显存和计算压力。工程上常通过 patch 化、window attention、factorized attention、稀疏 attention、latent 压缩等方式降低成本。

面试中可以总结为：**DiT 是用 Transformer 做扩散噪声预测主干的架构。视频生成转向 Transformer，是因为 Transformer 更适合大规模时空 token 建模、长程依赖和多模态条件融合；代价是 token 数量大时计算成本更高。**

<h2 id="面试问题u-net-和-dit-的核心差异是什么">面试问题：U-Net 和 DiT 的核心差异是什么？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

U-Net 和 DiT 的核心差异在于：**U-Net 是卷积式、多尺度、编码 - 解码结构；DiT 是 token 化、Transformer 式、全局注意力结构。**

可以从几个维度对比：

| 对比维度 | U-Net | DiT |
|----------|-------|-----|
| 基本单元 | 卷积、残差块、下采样/上采样 | Transformer block、Self-Attention、MLP |
| 数据表示 | 特征图 feature map | patch token / latent token |
| 建模方式 | 局部卷积 + 多尺度融合 | 全局 token 交互 |
| 条件注入 | timestep embedding、cross-attention、ControlNet 等 | AdaLN、FiLM、cross-attention、多模态 token 融合 |
| 优势 | 局部细节好、训练稳定、工程成熟 | 长程依赖强、扩展性好、适合大规模多模态建模 |
| 局限 | 全局建模和 scaling 能力有限 | token 多时计算和显存开销大 |

U-Net 更像是“图像处理专家”。它利用卷积归纳偏置处理局部纹理、边缘、形状和多尺度结构，对图像扩散非常有效。Stable Diffusion 早期版本就使用 U-Net 作为噪声预测主干。

DiT 更像是“通用 token 建模器”。它不强依赖卷积，而是把 latent patch 当作 token，通过 attention 建立全局依赖。这使它更适合高容量模型、长程一致性、多模态条件和视频时空建模。

在视频生成里，二者的差异会更加明显。U-Net 可以通过 3D 卷积或 temporal attention 扩展到视频，但通常仍带有较强的图像模型迁移痕迹；DiT 则可以更自然地把视频表示为时空 token，从一开始就做统一时空建模。

从工程选型看：

1. **资源有限、任务偏图像或短视频**：U-Net 更稳、更成熟。
2. **追求大规模生成质量和长程一致性**：DiT / Video Transformer 更有潜力。
3. **已有图像扩散模型需要快速视频化**：U-Net + temporal module 是常见路径。
4. **从头训练大规模视频基础模型**：DiT 类架构通常更符合趋势。

面试里可以回答：**U-Net 强在卷积多尺度去噪和局部细节恢复，DiT 强在 token 化全局建模和大规模扩展。视频生成从 U-Net 转向 DiT，本质上是从图像式局部建模走向统一时空 token 建模。**

<h2 id="面试问题fit-在视频生成架构中应该怎么理解">面试问题：FiT 在视频生成架构中应该怎么理解？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐ (3/5)**

FiT 在视频生成架构中可以理解为一种**更强调全图、全帧或完整 latent 表示的 Transformer 式扩散主干思想**。在不同语境中，FiT 的具体名称和实现可能略有差异，但在面试中更重要的是理解它代表的架构方向：**减少卷积式局部处理，更多依赖 Transformer 对完整视觉 token 序列进行统一建模。**

如果说 U-Net 是典型的多尺度卷积扩散主干，DiT 是标准的 patch-token Transformer 扩散主干，那么 FiT 可以从更工程化的角度理解为：**面向完整图像或完整视频帧 latent 的 Transformer 建模方式，强调全局上下文、统一 token 表示和高分辨率生成能力。**

在视频生成中，FiT 类思想通常包含几个特征：

1. **全帧 token 化**：把每一帧或视频 latent 划分为 token，不再只依赖局部卷积特征图。
2. **统一时空建模**：通过 Transformer 同时处理空间 token 和时间 token。
3. **高分辨率友好**：更关注全局结构、构图和长距离依赖，对复杂画面生成更有优势。
4. **条件融合自然**：文本、图像、音频、首帧等条件都可以作为 token 或调制信号进入主干。

一个简化理解是：

```text
Video Latent -> Patch / Frame Tokens -> Transformer Blocks -> Noise / Velocity Prediction
```

FiT 和 DiT 的关系可以这样把握：**DiT 是扩散 Transformer 的典型范式，FiT 更强调完整视觉表示或全图/全帧级 Transformer 建模。** 二者不是完全割裂的概念，而是都属于“扩散模型 Transformer 化”的方向。

需要注意的是，面试中不要把 FiT 简单说成“某个固定结构等价于 DiT”。更稳妥的说法是：FiT 体现的是 full-image / full-frame Transformer 的建模思想，它强调把完整视觉内容纳入统一 Transformer 表示中，从而提升全局建模和高分辨率生成能力。

它的挑战同样来自 token 数量。视频的 token 数量通常可以近似理解为：

$$
N \approx \frac{T}{p_t} \cdot \frac{H}{p_h} \cdot \frac{W}{p_w}
$$

其中 $p_t, p_h, p_w$ 分别是时间和空间 patch 尺度。随着帧数和分辨率增加，Transformer 的计算成本会快速上升。

面试里可以总结为：**FiT 可以理解为全图或全帧 Transformer 式扩散主干，强调完整视觉 token 序列上的统一建模。它和 DiT 同属扩散 Transformer 化趋势，优势是全局建模和高分辨率表达更强，挑战是视频 token 数量巨大、计算成本高。**

<h2 id="面试问题adaln--film-类调制为什么在-dit-中很常见">面试问题：AdaLN / FiLM 类调制为什么在 DiT 中很常见？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

AdaLN / FiLM 类调制在 DiT 中很常见，核心原因是：**Transformer 主干需要一种稳定、轻量、全局有效的方式注入扩散时间步和条件信息，而归一化调制非常适合做这件事。**

FiLM（Feature-wise Linear Modulation）的基本形式是对特征做通道级缩放和平移：

$$
\mathrm{FiLM}(h) = \gamma(c) \odot h + \beta(c)
$$

其中 $h$ 是中间特征， $c$ 是条件向量， $\gamma(c)$ 和 $\beta(c)$ 分别由条件生成。

AdaLN（Adaptive LayerNorm）则通常是在 LayerNorm 后进行条件调制：

$$
\mathrm{AdaLN}(h, c)
= \gamma(c) \odot \mathrm{LN}(h) + \beta(c)
$$

在 DiT 中，条件 $c$ 往往由扩散时间步 embedding、文本条件、类别条件或其他全局条件组合而成。模型通过一个小的 MLP 预测每个 Transformer block 所需的调制参数。

这样做有几个好处：

1. **适合 Transformer 结构**：Transformer 本身大量使用 LayerNorm，直接调制 LN 后的特征非常自然。
2. **条件注入稳定**：相比直接拼接条件 token，调制方式更轻量，不会显著增加 token 长度。
3. **全局控制能力强**：扩散时间步、类别、风格等全局条件可以影响整个 block 的计算。
4. **便于扩展多条件**：文本 embedding、timestep embedding、类别 embedding 可以融合后统一生成调制参数。
5. **训练更稳定**：许多 DiT 实现会使用 AdaLN-Zero 等初始化策略，让模型初始状态接近恒等映射，降低训练不稳定风险。

在扩散模型中，时间步 $t$ 是非常关键的条件。因为不同噪声等级下，模型要执行的去噪行为不同：早期步骤更关注全局结构，后期步骤更关注细节恢复。AdaLN / FiLM 可以让每一层根据 $t$ 动态调整特征处理方式。

对于文本或多模态条件，AdaLN / FiLM 通常和 cross-attention 互补使用：cross-attention 适合 token 级语义读取，AdaLN / FiLM 适合全局风格、时间步、类别或整体条件调制。

面试中可以回答：**AdaLN / FiLM 是 DiT 中常见的条件注入机制，它通过条件生成缩放和平移参数来调制 Transformer 特征。这样既能稳定注入 timestep 和全局条件，又不会显著增加 token 数量，是 DiT 类扩散模型中非常重要的条件控制手段。**

---

<h1 id="5.视频扩散的特殊改造时序建模时空注意力一致性">5.视频扩散的特殊改造：时序建模、时空注意力、一致性</h1>

### 🔑 视频特殊挑战
- **时序连续性**：相邻帧之间要平滑过渡。
- **运动一致性**：物体和镜头运动要符合合理动态。
- **长程依赖**：远距离帧之间要保持身份、场景和语义一致。

<h2 id="面试问题为什么不能直接把图像扩散模型简单扩展到视频">面试问题：为什么不能直接把图像扩散模型简单扩展到视频？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

不能直接把图像扩散模型简单扩展到视频，核心原因是：**视频不是独立图像的集合，而是带有时间依赖、运动规律和跨帧一致性约束的时空序列。**

如果只把图像扩散模型逐帧应用到视频上，模型可能每一帧看起来都不错，但连续播放时会出现明显问题，例如主体身份变化、纹理闪烁、背景跳动、动作不连续等。这些问题本质上来自模型缺少时间维度建模。

图像扩散通常处理的是：

$$
x \in \mathbb{R}^{C \times H \times W}
$$

而视频扩散需要处理：

$$
x \in \mathbb{R}^{T \times C \times H \times W}
$$

多出来的 $T$ 维度不只是数据量增加，更意味着模型要理解帧间关系、运动方向、遮挡变化和长程依赖。

直接扩展图像扩散模型会遇到几个核心问题：

1. **计算成本急剧增加**：视频比图像多时间维度，token 数量和显存开销都会成倍增长。
2. **缺少运动建模**：图像模型只知道单帧外观，不知道目标如何随时间移动。
3. **时序一致性不足**：逐帧生成容易造成 flicker、identity drift 和背景漂移。
4. **长程依赖难捕捉**：人物或场景在远距离帧之间需要保持一致。
5. **条件控制更复杂**：文本、首帧、动作、相机运动等条件都需要跨时间生效。

因此视频扩散通常要在图像扩散基础上做特殊改造，例如引入 temporal convolution、temporal attention、3D U-Net、space-time attention、motion module、3D RoPE、视频 VAE 或专门的 Video DiT。

面试里可以回答：**图像扩散模型只解决单帧生成，视频扩散还要解决时序关系、运动一致性和长程依赖。简单逐帧扩展会导致闪烁、漂移和动作不连续，因此必须引入专门的时序建模和跨帧一致性机制。**

<h2 id="面试问题视频扩散模型里常见的时序建模方法有哪些">面试问题：视频扩散模型里常见的时序建模方法有哪些？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

视频扩散模型里的时序建模方法，本质上都是为了解决一个问题：**让模型在生成每一帧时不仅看当前帧，还能利用前后帧信息，从而生成连续、稳定、有运动逻辑的视频。**

常见方法可以分为几类：

| 方法 | 核心思路 | 代表特点 |
|------|----------|----------|
| 3D 卷积 | 在时间和空间维度上同时做卷积 | 局部时空建模强，适合短程运动 |
| Temporal Attention | 让同一空间位置或 token 在不同帧之间做 attention | 强化跨帧一致性和动作连续性 |
| Space-Time Attention | 同时建模空间 token 和时间 token 的关系 | 表达能力强，但计算成本高 |
| Factorized Attention | 将空间 attention 和时间 attention 分开计算 | 降低复杂度，工程上常用 |
| Motion Module | 在已有图像扩散模型中插入专门的运动模块 | 便于从图像模型迁移到视频模型 |
| Recurrent / Causal Modeling | 当前帧只依赖历史帧或历史 latent | 适合流式生成和长视频扩展 |

如果用 attention 表达，时序 attention 可以理解为让第 $t$ 帧的特征关注其他帧：

$$
\mathrm{Attention}(Q_t, K_{1:T}, V_{1:T})
= \mathrm{softmax}\left(\frac{Q_t K_{1:T}^{\top}}{\sqrt{d}}\right)V_{1:T}
$$

这意味着当前帧在生成时可以参考其他时间位置的内容，从而保持主体、背景和运动趋势一致。

在实际模型中，很多方法会组合使用这些机制。例如 AnimateDiff 在图像扩散模型中插入 motion module；VideoCrafter、Stable Video Diffusion 等模型会结合视频 latent、temporal attention 和时空 U-Net；更大规模的 Video DiT 则通常把视频表示成时空 token，再用 Transformer 做统一建模。

面试中可以总结为：**视频扩散的时序建模方法主要包括 3D 卷积、Temporal Attention、Space-Time Attention、Factorized Attention、Motion Module 和 Causal Modeling。它们的共同目标是让模型显式建模帧间关系，减少 flicker、drift 和动作不连续。**

<h2 id="面试问题时空注意力和纯空间注意力有什么区别">面试问题：时空注意力和纯空间注意力有什么区别？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

纯空间注意力只在单帧内部建模 token 之间的关系，而时空注意力会同时考虑空间维度和时间维度，让不同帧之间的 token 也能交互。

对于一段视频，如果每帧有 $N_s$ 个空间 token，总共有 $T$ 帧，那么纯空间注意力通常是在每一帧内部做：

$$
\mathrm{Attn}_{space}: \quad N_s \times N_s
$$

它关注的是单帧中的物体、背景、布局和纹理关系，但不同帧之间没有直接交互。

时空注意力则把时间维度也纳入建模。最直接的 full space-time attention 会在所有视频 token 上做 attention：

$$
N = T \cdot N_s
$$

其复杂度近似为：

$$
\mathcal{O}(N^2) = \mathcal{O}(T^2 N_s^2)
$$

这种方式表达能力强，但对长视频和高分辨率视频非常昂贵。

工程上更常见的是分解式时空注意力：

1. **空间 attention**：每帧内部做 attention，建模单帧画面结构。
2. **时间 attention**：同一空间位置或对应 token 跨帧做 attention，建模运动和一致性。

可以概括为：

```text
纯空间注意力：只回答“这一帧内部怎么组织”
时空注意力：同时回答“这一帧是什么”和“前后帧怎么变化”
```

时空注意力的优势是能更好捕捉运动、遮挡、身份保持和长程依赖；缺点是 token 数量大、计算成本高，需要 window attention、factorized attention 或稀疏 attention 等优化。

面试里可以回答：**纯空间注意力只建模单帧内部关系，时空注意力会把跨帧关系也纳入 attention。视频生成必须关心运动和一致性，因此通常需要 temporal attention 或 space-time attention，而不能只依赖纯空间注意力。**

<h2 id="面试问题什么是-3d-rope它在视频生成中有什么优势">面试问题：什么是 3D RoPE？它在视频生成中有什么优势？</h2>

**难度评分：⭐⭐⭐⭐⭐ (5/5)  |  考察频率：⭐⭐⭐ (3/5)**

3D RoPE（3D Rotary Position Embedding）可以理解为：**把 RoPE 从一维序列位置扩展到视频的三维位置，即时间、高度、宽度三个维度，用于给时空 token 注入相对位置信息。**

普通 Transformer 需要位置编码，否则模型只知道 token 内容，不知道 token 在序列中的位置。图像模型需要二维空间位置，视频模型则需要三维时空位置：

$$
(t, h, w)
$$

其中 $t$ 表示帧位置， $h, w$ 表示空间位置。3D RoPE 的核心思想是把时间维、垂直空间维、水平空间维分别编码到 attention 的 query/key 中，使 attention 计算天然包含相对时空位置信息。

RoPE 的一维思想可以简化理解为：对 query 和 key 按位置做旋转变换：

$$
q_m = R_m q, \qquad k_n = R_n k
$$

attention 中的内积会包含相对位置 $m-n$ 的信息。3D RoPE 则把这个位置从 $m$ 扩展为 $(t,h,w)$，让模型感知 token 之间的时间距离和空间距离。

在视频生成中，3D RoPE 的优势主要有：

1. **同时表达时间和空间位置**：适合视频 token 的天然三维结构。
2. **增强相对位置建模**：模型不仅知道 token 在哪，还能知道 token 之间相隔多远。
3. **有利于运动建模**：时间维位置编码可以帮助模型理解动作顺序和帧间关系。
4. **适合长视频扩展**：相对位置编码通常比绝对位置表更有泛化潜力。
5. **与 Transformer / DiT 结构匹配**：Video DiT 中大量使用 attention，3D RoPE 能自然融入 query/key。

不过，3D RoPE 不是单独解决视频生成的全部问题。它提供的是时空位置感知能力，真正的运动一致性还需要网络结构、训练数据、条件控制和采样策略共同支撑。

面试中可以总结为：**3D RoPE 是面向视频时空 token 的旋转位置编码，把时间、高度、宽度三个维度的位置关系注入 attention。它能增强 Video Transformer 对空间结构、时间顺序和相对运动关系的建模能力。**

<h2 id="面试问题视频扩散模型中的时间一致性问题具体指什么">面试问题：视频扩散模型中的时间一致性问题具体指什么？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

视频扩散模型中的时间一致性问题，指的是：**生成视频在连续帧之间是否保持稳定、自然、连贯，是否避免无意义的视觉或语义跳变。**

它至少包含几个层面：

| 一致性类型 | 含义 | 典型失败表现 |
|------------|------|--------------|
| 内容一致性 | 场景、物体、布局保持稳定 | 背景跳变、物体突然消失 |
| 身份一致性 | 同一主体外观和身份保持一致 | 人脸变化、衣服颜色漂移 |
| 运动一致性 | 动作轨迹、速度、方向自然连续 | 动作抽搐、反向跳跃 |
| 视觉一致性 | 颜色、亮度、纹理、边缘稳定 | flicker、纹理闪烁 |
| 条件一致性 | 多帧持续符合文本或控制条件 | 前几帧符合 prompt，后面跑偏 |

从特征约束角度看，一种常见思想是让相邻帧在运动补偿后保持接近。例如用运动场 $F_{t \rightarrow t+1}$ 将第 $t$ 帧特征 warp 到第 $t+1$ 帧：

$$
\mathcal{L}_{tc}
= \sum_t
\left\|
\phi(x_{t+1}) - \mathcal{W}(\phi(x_t), F_{t \rightarrow t+1})
\right\|_1
$$

其中 $\phi$ 是图像或特征提取器， $\mathcal{W}$ 表示 warp 操作。这个损失表达的是：如果考虑运动补偿，前后帧的内容应该尽量一致。

时间一致性问题在视频扩散中很常见，因为扩散模型每一步都在高维噪声空间中逐步生成内容，如果时序建模不足，不同帧可能在去噪过程中走向不同的细节解，最终表现为闪烁和漂移。

面试里可以回答：**时间一致性就是要求视频在内容、身份、运动和视觉属性上跨帧稳定。它不是单帧质量问题，而是连续播放时的时序稳定性问题，是视频生成区别于图像生成的核心难点。**

<h2 id="面试问题视频生成为什么更容易出现-flicker-和-drift">面试问题：视频生成为什么更容易出现 flicker 和 drift？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

视频生成更容易出现 flicker 和 drift，是因为视频生成不是一次性生成单帧，而是在时间维度上生成一串相关帧；只要跨帧约束不足，局部误差就会在播放时被人眼放大。

**Flicker** 通常指相邻帧之间亮度、颜色、纹理、边缘或细节不稳定，表现为闪烁、抖动、忽清忽糊。
**Drift** 通常指内容随时间逐渐跑偏，表现为身份变化、主体形状变化、背景结构漂移或动作越来越不符合条件。

主要原因包括：

1. **逐帧生成或弱时序建模**：每帧独立去噪，细节随机性不同，容易闪烁。
2. **噪声采样带来的随机差异**：扩散采样本身具有随机性，不同帧可能收敛到不同细节。
3. **运动和遮挡建模不足**：快速运动、遮挡、形变区域最容易出现伪影。
4. **长程依赖不足**：模型忘记前面帧的人物身份、服装、场景结构。
5. **误差积累**：长视频或自回归视频生成中，前面的小误差会传递到后面。
6. **条件注入不稳定**：不同帧对 prompt 或参考图的关注不一致。

如果用误差传播理解 drift，可以简化写成：

$$
\hat{x}_t = f(\hat{x}_{t-1}, c_t)
$$

当前一帧预测存在误差时：

$$
e_t \approx f(\hat{x}_{t-1}, c_t) - f(x_{t-1}, c_t)
$$

误差会随着时间继续影响后续帧，从而形成逐渐跑偏。

面试中可以总结为：**flicker 是短时跨帧视觉不稳定，drift 是长时内容逐渐跑偏。二者都源于时序建模不足、噪声随机性、运动理解不准确和长程一致性约束不强。**

---

<h1 id="6.视频自动标注模型与数据流水线">6.视频自动标注模型与数据流水线</h1>

### 🔑 标注方法分类
| 类型 | 代表方向 | 主要用途 |
|------|----------|----------|
| 规则与传统模型 | 镜头检测、动作识别、目标检测 | 生成结构化标签 |
| 图文多模态模型 | CLIP、BLIP、InstructBLIP | 生成语义标签和描述 |
| 视频多模态模型 | Video-LLaMA、VideoChat、TimeChat | 理解时序事件和视频内容 |

<h2 id="面试问题什么是视频自动标注它在视频生成中有什么作用">面试问题：什么是视频自动标注？它在视频生成中有什么作用？</h2>

**难度评分：⭐⭐ (2/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

视频自动标注是指：**利用模型或规则系统自动为视频生成文本描述、动作标签、场景标签、主体标签、时间片段标签或质量标签的过程。**

在视频生成中，自动标注的核心作用是构建高质量训练数据。大规模视频生成模型通常需要海量视频 - 文本对，如果没有自动标注，很难人工完成数据描述。

一个典型数据流水线可以概括为：

```text
原始视频 -> 切分镜头/片段 -> 抽帧/采样 -> 多模型理解
        -> 生成 caption/tag -> 质量过滤 -> 训练数据
```

自动标注通常会输出多种信息：

| 标注类型 | 示例 | 用途 |
|----------|------|------|
| 全局 caption | 一个人在海边奔跑，夕阳背景 | 文本到视频训练 |
| 动作标签 | running、turning、jumping | 动作控制和运动学习 |
| 主体标签 | person、dog、car | 主体语义对齐 |
| 场景标签 | beach、street、indoor | 背景和场景建模 |
| 时间片段标签 | 第 2-4 秒人物转身 | 时序事件理解 |
| 质量标签 | 清晰度、是否水印、是否抖动 | 数据过滤 |

高质量自动标注能直接影响模型的 prompt following 能力。标注越准确，模型越容易学会文本和视频内容之间的对应关系；标注噪声越大，模型越容易出现语义不对齐、动作理解差、生成内容泛化差等问题。

面试里可以回答：**视频自动标注是用模型自动生成视频描述和结构化标签的过程，它是大规模视频生成训练数据构建的关键环节。标注质量会直接影响模型的文本对齐、动作理解和生成可控性。**

<h2 id="面试问题视频自动标注和图像自动标注有什么区别">面试问题：视频自动标注和图像自动标注有什么区别？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

视频自动标注和图像自动标注的核心区别在于：**图像标注主要描述静态内容，而视频标注还必须描述动作、顺序、事件变化和时间范围。**

图像标注通常关注：

- 画面中有什么对象
- 场景是什么
- 物体属性和空间关系如何
- 风格、构图、光照如何

视频标注除了这些，还要关注：

- 主体在做什么动作
- 动作发生的先后顺序
- 事件从何时开始、何时结束
- 镜头是否运动
- 物体是否出现、消失、遮挡或交互

可以对比如下：

| 维度 | 图像自动标注 | 视频自动标注 |
|------|--------------|--------------|
| 数据单位 | 单张图片 | 多帧片段或完整视频 |
| 核心语义 | 对象、属性、场景 | 对象、动作、事件、时序 |
| 难点 | 细粒度对象和属性识别 | 动作顺序、事件阶段、长程依赖 |
| 常用方法 | 图像分类、检测、图像 caption | 抽帧理解、视频模型、多模态 LLM、时序定位 |

一个图像 caption 可能是“一个人在厨房里”。
一个视频 caption 则应该更完整，例如“一个人在厨房里拿起杯子，倒入牛奶，然后放回桌上”。后者包含动作、顺序和过程。

面试中可以总结为：**图像自动标注偏静态语义，视频自动标注偏动态语义和时序事件。视频标注更难，因为它不仅要看懂每一帧，还要理解帧与帧之间发生了什么变化。**

<h2 id="面试问题主流视频自动标注方法可以分为哪几类">面试问题：主流视频自动标注方法可以分为哪几类？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐ (3/5)**

主流视频自动标注方法通常可以分为四类：规则与传统视觉模型、图文多模态模型、视频专用多模态模型，以及多模型流水线集成。

| 方法类型 | 代表方法 | 优势 | 局限 |
|----------|----------|------|------|
| 规则与传统视觉模型 | 镜头检测、OCR、目标检测、动作识别 | 稳定、可控、适合结构化标签 | 语义表达有限 |
| 图文多模态模型 | CLIP、BLIP、InstructBLIP、LLaVA 抽帧 | 语义描述能力强，工程易用 | 对时序事件理解弱 |
| 视频多模态模型 | Video-LLaMA、VideoChat、TimeChat、Video-ChatGPT | 能理解动作和时序过程 | 成本高，标注质量依赖模型能力 |
| 多模型流水线 | 检测 + OCR + VLM + LLM 清洗 | 综合质量高，适合大规模数据治理 | 工程复杂，延迟较高 |

实际工业流水线通常不是只用一个模型，而是组合多个模型。例如：

```text
镜头切分 -> 抽关键帧 -> CLIP/BLIP 生成静态描述
        -> 视频模型生成动作描述 -> LLM 汇总和清洗 caption
        -> 质量模型过滤低质样本
```

这样做的好处是可以兼顾静态内容、动态动作和数据质量。比如目标检测模型负责主体标签，OCR 模型负责文字水印检查，视频多模态模型负责动作和事件，LLM 负责把多路信息整理成自然语言 caption。

面试里可以回答：**视频自动标注通常分为规则/传统模型、图文多模态模型、视频专用多模态模型和多模型流水线。大规模视频生成训练中，更常见的是多模型组合，以同时获得主体、场景、动作、时序和质量标签。**

<h2 id="面试问题clipblip-系列模型如何用于视频自动标注">面试问题：CLIP、BLIP 系列模型如何用于视频自动标注？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

CLIP、BLIP 系列模型主要通过**抽帧或关键帧理解**的方式用于视频自动标注。它们本质上更偏图像或图文多模态模型，因此通常先从视频中抽取若干帧，再对这些帧做语义理解。

CLIP 更适合做图文匹配、标签筛选和相似度过滤。给定视频帧 $I$ 和文本候选 $t$，CLIP 可以计算相似度：

$$
s(I, t) = \frac{f_I(I)^\top f_T(t)}{\|f_I(I)\|\;\|f_T(t)\|}
$$

其中 $f_I$ 是图像编码器， $f_T$ 是文本编码器。通过相似度可以判断视频是否符合某些标签，例如 indoor、outdoor、dog、car、cartoon style 等。

BLIP / BLIP-2 / InstructBLIP 更适合生成自然语言描述。常见做法是：

1. 从视频中抽取首帧、中间帧、关键帧。
2. 对每帧生成 caption 或问答结果。
3. 汇总多帧 caption，得到视频级描述。
4. 用 LLM 清洗重复内容，补充动作和顺序表达。

这种方法的优势是工程简单、成本较低、可以复用成熟图像多模态模型。局限是它对视频的时间维度理解有限，容易把视频描述成若干静态图像，而忽略动作过程。

因此实际系统中，CLIP/BLIP 往往负责静态语义标签和初始 caption，再由视频模型或 LLM 补充动作、时序和事件信息。

面试里可以总结为：**CLIP 适合做视频帧的图文匹配、标签筛选和质量过滤，BLIP 系列适合基于关键帧生成 caption。它们是视频自动标注的重要基础模块，但单独使用时对动作顺序和时序事件理解不足。**

<h2 id="面试问题视频专用多模态模型在自动标注中有什么价值">面试问题：视频专用多模态模型在自动标注中有什么价值？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐ (3/5)**

视频专用多模态模型的价值在于：**它们能够直接处理多帧输入，理解动作、事件、顺序和时间范围，比单纯抽帧的图像模型更适合生成视频级标注。**

视频专用模型通常会在视觉编码阶段保留时间维度，或者通过 temporal attention、video tokens、frame embeddings 等机制建模帧间关系。因此它们更擅长回答：

- 谁在做什么动作
- 动作先后顺序是什么
- 事件持续了多久
- 哪个时间段发生了关键变化
- 镜头是否移动
- 主体是否发生交互

典型方向包括 Video-LLaMA、VideoChat、TimeChat、Video-ChatGPT、InternVideo、VideoCLIP 等。它们可以用于生成更完整的视频 caption，例如：

```text
一个人走进厨房，拿起桌上的杯子，倒入饮料，然后转身离开。
```

相比抽帧 caption，这类描述更像视频数据训练真正需要的 prompt，因为它包含动作和事件过程。

但视频专用模型也有成本和质量问题。它们通常推理更慢、显存更高，对长视频需要采样或分段处理；如果模型能力不足，也可能产生幻觉，比如描述视频中不存在的动作。

面试中可以回答：**视频专用多模态模型的核心价值是理解动态语义和时序事件，能生成比抽帧图像模型更符合视频训练需求的 caption。但它们成本更高，也需要配合质量过滤和人工/模型校验来控制幻觉。**

<h2 id="面试问题如何评估视频自动标注的质量">面试问题：如何评估视频自动标注的质量？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐ (3/5)**

评估视频自动标注质量，不能只看句子是否通顺，而要看标注是否准确覆盖视频内容、动作、时序和条件训练需要。

常见评估维度包括：

| 维度 | 含义 | 常见问题 |
|------|------|----------|
| 准确性 | 描述是否符合视频真实内容 | 把不存在的物体或动作写进去 |
| 完整性 | 是否覆盖主体、动作、场景和关键事件 | 只描述背景，漏掉核心动作 |
| 时序性 | 是否表达动作顺序和事件过程 | 顺序错误，把结果写在原因前 |
| 细粒度 | 是否包含有用属性和控制信息 | 描述过粗，不利于模型学习 |
| 简洁性 | 是否避免冗余和无关内容 | caption 太长、重复、噪声大 |
| 一致性 | 多帧、多模型标注是否互相矛盾 | 前后描述主体不一致 |

自动化评估可以使用 CLIPScore、视频 - 文本相似度模型、caption 质量模型、规则检查、水印检测、OCR 检测等；人工评估则通常抽样检查 caption 是否忠实、动作是否准确、是否存在 hallucination。

在训练数据场景中，更实用的评估方式是看下游效果：使用该标注训练的视频生成模型，是否提升 prompt following、动作生成、场景一致性和整体质量。

面试里可以总结为：**视频自动标注质量要从准确性、完整性、时序性、细粒度和一致性评估。好的标注不仅要描述画面有什么，还要描述动作如何发生，并且不能引入幻觉和噪声。**

---

<h1 id="7.视频插帧模型vfi的核心方法与选型">7.视频插帧模型：VFI 的核心方法与选型</h1>

### 🔑 插帧方法对比
| 方法类型 | 代表方向 | 优势 | 局限 |
|----------|----------|------|------|
| 光流法 | Flow-based VFI | 运动建模直观 | 遮挡和大运动较难 |
| CNN 法 | DAIN、RIFE、AMT | 推理效率高 | 长程依赖建模有限 |
| Transformer 法 | VFIT 等 | 全局依赖建模强 | 计算成本较高 |

<h2 id="面试问题什么是视频插帧它有哪些典型应用场景">面试问题：什么是视频插帧？它有哪些典型应用场景？</h2>

**难度评分：⭐⭐ (2/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

视频插帧（Video Frame Interpolation，VFI）指的是：**给定前后两帧或多帧视频，预测中间时刻不存在的帧，从而提高视频帧率或生成更平滑的运动。**

最基本的问题可以写成：

$$
\hat{I}_t = F(I_0, I_1, t), \qquad 0 < t < 1
$$

其中 $I_0$ 和 $I_1$ 是已知前后帧， $\hat{I}_t$ 是中间时刻 $t$ 的预测帧。

典型应用场景包括：

1. **帧率提升**：把 24 FPS、30 FPS 视频提升到 60 FPS 或 120 FPS。
2. **慢动作生成**：在原视频帧之间插入更多帧，获得顺滑慢动作效果。
3. **视频修复**：补全丢帧、卡顿或不连续片段。
4. **动画补帧**：减少手绘动画或生成动画中的跳动感。
5. **视频生成后处理**：提升 AI 生成视频的流畅度和观感。

面试里可以回答：**视频插帧就是根据已有帧预测中间帧，本质是时序补全任务。它常用于帧率提升、慢动作、丢帧修复、动画补帧和视频生成后处理。**

<h2 id="面试问题视频插帧的核心挑战是什么">面试问题：视频插帧的核心挑战是什么？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

视频插帧的核心挑战是：**模型不仅要知道前后帧长什么样，还要准确推断中间时刻物体应该在哪里、遮挡关系如何变化、细节如何生成。**

主要难点包括：

| 挑战 | 说明 |
|------|------|
| 大运动 | 前后帧位移很大时，中间位置难以准确估计 |
| 遮挡和显露 | 物体被遮挡或重新出现时，中间内容没有直接对应像素 |
| 非刚体形变 | 人体、衣物、水流、头发等形变复杂 |
| 运动模糊 | 原始帧不清晰，运动边界难判断 |
| 细节合成 | 中间帧可能需要生成新纹理或边缘 |
| 时序一致性 | 多次插帧后不能产生抖动或伪影积累 |

插帧看似只是“补一张图”，但本质上依赖运动估计和内容重建。简单线性融合容易产生重影，因为物体在两帧中的位置不同，直接平均会把两个位置都叠出来。

面试中可以总结为：**视频插帧最难的是运动估计、遮挡处理和细节重建。优秀的 VFI 模型要同时解决“中间帧物体在哪里”和“被遮挡或新出现的内容怎么生成”。**

<h2 id="面试问题基于光流的插帧方法基本原理是什么">面试问题：基于光流的插帧方法基本原理是什么？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

基于光流的插帧方法，核心思想是：**先估计前后帧之间的像素运动，再根据目标时间点把两端图像 warp 到中间时刻，最后融合得到插帧结果。**

给定两帧 $I_0$ 和 $I_1$，假设估计出从 $I_0$ 到 $I_1$ 的光流：

$$
F_{0 \rightarrow 1}
$$

以及反向光流：

$$
F_{1 \rightarrow 0}
$$

对于中间时刻 $t$，可以近似得到中间光流：

$$
F_{t \rightarrow 0} \approx -t F_{0 \rightarrow 1}
$$

$$
F_{t \rightarrow 1} \approx (1-t) F_{0 \rightarrow 1}
$$

然后通过 backward warping 从两端帧采样中间内容：

$$
\hat{I}_{t \leftarrow 0} = \mathcal{W}(I_0, F_{t \rightarrow 0})
$$

$$
\hat{I}_{t \leftarrow 1} = \mathcal{W}(I_1, F_{t \rightarrow 1})
$$

最后通过融合网络或遮挡 mask 得到最终结果：

$$
\hat{I}_t = M \odot \hat{I}_{t \leftarrow 0} + (1-M) \odot \hat{I}_{t \leftarrow 1}
$$

其中 $M$ 是融合 mask，用来处理遮挡、显露和置信度差异。

这种方法的优点是运动建模直观、可解释性强；缺点是光流估计一旦错误，插帧结果容易出现重影、扭曲、边缘破碎，尤其在大运动、遮挡和复杂非刚体运动中更明显。

面试里可以回答：**基于光流的插帧先估计前后帧运动，再把两端帧 warp 到中间时刻并融合。它的关键是光流估计、遮挡处理和融合 mask，优点是直观，难点是大运动和遮挡场景下容易出错。**

<h2 id="面试问题rifedainamt-等插帧模型的主要差异是什么">面试问题：RIFE、DAIN、AMT 等插帧模型的主要差异是什么？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐ (3/5)**

RIFE、DAIN、AMT 都是视频插帧领域常见模型，但它们的设计重点不同。

| 模型 | 核心思想 | 主要特点 |
|------|----------|----------|
| DAIN | Depth-Aware Video Frame Interpolation | 引入深度信息辅助处理遮挡和前后层次 |
| RIFE | Real-Time Intermediate Flow Estimation | 直接估计中间流，强调实时、高效、工程可用 |
| AMT | All-Pairs Multi-Field Transforms | 更强调全局匹配、多场变换和复杂运动处理 |

DAIN 的关键思想是利用深度估计辅助插帧。因为遮挡关系通常和深度有关，前景物体更可能遮挡背景。引入 depth-aware flow projection 后，模型能更好处理遮挡边界，但系统复杂度较高。

RIFE 的关键思想是直接估计中间时刻相关的 flow，而不是先完整估计前后双向光流再复杂投影。它结构相对轻量，推理速度快，因此常用于实时插帧和工程应用。

AMT 更关注复杂运动和大位移场景，通过更强的匹配和多场变换机制提升插帧质量，通常质量更好但计算成本也更高。

从面试角度看，不需要背所有结构细节，但要抓住差异：DAIN 偏深度和遮挡，RIFE 偏实时中间流估计，AMT 偏高质量复杂运动匹配。

面试里可以总结为：**DAIN 用深度辅助遮挡处理，RIFE 强调实时高效地估计中间流，AMT 更强调复杂运动下的高质量匹配和多场变换。选型时通常在速度、质量和复杂运动鲁棒性之间权衡。**

<h2 id="面试问题多尺度融合为什么对视频插帧很重要">面试问题：多尺度融合为什么对视频插帧很重要？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐ (3/5)**

多尺度融合对视频插帧很重要，原因是：**插帧同时需要处理大范围运动和局部细节，而单一尺度很难兼顾这两者。**

在粗尺度上，模型可以更容易估计大运动和整体结构，因为分辨率低、搜索范围相对更大；在细尺度上，模型可以恢复边缘、纹理、轮廓和小物体细节。

典型多尺度流程可以概括为：

```text
低分辨率估计大致运动
-> 中分辨率细化位移和结构
-> 高分辨率恢复边缘和纹理
```

多尺度融合的价值包括：

1. **提高大运动鲁棒性**：低分辨率下更容易捕捉大位移。
2. **减少重影和错位**：逐层细化 flow 或特征对齐。
3. **恢复局部细节**：高分辨率分支保留纹理和边界。
4. **提升遮挡处理能力**：不同尺度能提供互补上下文。

许多插帧模型都会采用 pyramid、coarse-to-fine、multi-scale feature fusion 或 multi-level refinement。RIFE、AMT 等模型也都包含不同形式的多尺度估计或融合思想。

面试中可以回答：**多尺度融合让模型先在粗尺度处理大运动，再在细尺度恢复细节，是插帧中兼顾运动范围和画面质量的关键设计。**

<h2 id="面试问题视频插帧模型常用哪些评估指标">面试问题：视频插帧模型常用哪些评估指标？</h2>

**难度评分：⭐⭐ (2/5)  |  考察频率：⭐⭐⭐ (3/5)**

视频插帧模型常用评估指标可以分为客观指标、感知指标和效率指标。

| 指标 | 含义 | 越高/越低 |
|------|------|-----------|
| PSNR | 像素级重建误差指标 | 越高越好 |
| SSIM | 结构相似性指标 | 越高越好 |
| LPIPS | 深度特征感知距离 | 越低越好 |
| IE / Interpolation Error | 插帧误差 | 越低越好 |
| VFIPS / FVD | 视频感知质量或分布距离 | 通常越低越好 |
| Runtime / FPS | 推理速度 | FPS 越高越好 |
| Params / FLOPs | 模型规模和计算量 | 视应用场景而定 |

PSNR 和 SSIM 更关注像素和结构一致性，但不一定完全符合人眼感知；LPIPS 更关注感知质量；视频级指标则更关注时序稳定性和整体观感。实际评估中通常还需要人工观察，因为插帧模型可能 PSNR 不低，但边缘重影、局部扭曲或播放观感不好。

对于工程应用，速度也非常关键。实时插帧场景往往更关注 FPS、显存和延迟；影视修复或离线增强场景则可以牺牲速度换取更高质量。

面试里可以总结为：**插帧常用 PSNR、SSIM、LPIPS、插帧误差、视频感知指标和运行速度评估。客观指标只能反映一部分质量，实际还要看重影、遮挡区域、运动边界和播放流畅度。**

---

<h1 id="8.ai视频生成基础架构总览">8.AI 视频生成基础架构总览</h1>

### 🔑 核心架构流程
```text
输入条件 -> 条件编码器 -> 视频潜空间 -> 扩散主干网络
        -> 迭代去噪 -> VAE 解码器 -> 输出视频
```

<h2 id="面试问题ai-视频生成系统的核心工作流程是什么">面试问题：AI 视频生成系统的核心工作流程是什么？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

AI 视频生成系统的核心工作流程可以概括为：**把用户条件编码成语义特征，把视频压缩到潜空间，在潜空间中通过扩散或 Transformer 主干逐步生成视频 latent，最后由 VAE 解码成像素视频。**

典型流程如下：

```text
文本/图像/音频/首帧条件
-> 条件编码器
-> 初始化视频 latent
-> 扩散主干网络迭代去噪
-> VAE Decoder 解码
-> 后处理与导出
```

更具体地说：

1. **条件输入**：用户提供 prompt、参考图、首帧、音频、姿态、轨迹等。
2. **条件编码**：T5、CLIP、音频编码器、姿态编码器等把条件转成 embedding。
3. **潜空间初始化**：从高斯噪声初始化视频 latent，或由参考图/首帧编码得到部分 latent。
4. **迭代去噪**：U-Net、DiT 或 Video Transformer 在 scheduler 控制下逐步去噪。
5. **VAE 解码**：把生成好的 latent 解码回 RGB 视频。
6. **后处理**：插帧、超分、去闪烁、编码压缩、格式导出等。

面试里可以回答：**AI 视频生成系统通常是条件编码 + latent diffusion / transformer 去噪 + VAE 解码的流水线。核心不是一次生成像素，而是在压缩潜空间中迭代生成，再解码成视频。**

<h2 id="面试问题ai-视频生成的核心组件通常包括哪些">面试问题：AI 视频生成的核心组件通常包括哪些？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

AI 视频生成系统的核心组件通常包括条件编码器、VAE / Video VAE、扩散主干网络、噪声调度器、采样器和后处理模块。

| 组件 | 作用 | 常见实现 |
|------|------|----------|
| 条件编码器 | 编码文本、图像、音频、动作等输入条件 | T5、CLIP、音频模型、Pose Encoder |
| VAE / Video VAE | 像素视频和 latent 之间转换 | 2D VAE、3D VAE、Causal Video VAE |
| 扩散主干网络 | 在 latent space 中预测噪声或 velocity | U-Net、Video U-Net、DiT、Video Transformer |
| 噪声调度器 | 控制加噪和去噪时间步 | DDPM、DDIM、DPM-Solver、Euler |
| 条件注入模块 | 将条件注入生成主干 | Cross-Attention、AdaLN、FiLM、ControlNet |
| 后处理模块 | 提升最终视频质量 | 插帧、超分、去闪烁、编码器 |

其中最核心的是三件事：**条件如何理解、视频如何压缩、latent 如何生成。** 条件编码器决定模型是否听懂用户意图；VAE 决定压缩和解码质量；扩散主干决定生成质量、运动能力和一致性。

面试中可以总结为：**AI 视频生成系统通常由条件编码器、Video VAE、扩散/Transformer 主干、噪声调度器、条件注入模块和后处理模块组成。工程选型时要重点关注条件对齐、潜空间质量、时序建模和推理成本。**

<h2 id="面试问题vae-编码器和解码器在视频生成中分别负责什么">面试问题：VAE 编码器和解码器在视频生成中分别负责什么？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

VAE 编码器和解码器分别负责**压缩**和**还原**。编码器把像素视频映射到潜空间，解码器把生成后的 latent 还原成 RGB 视频。

编码过程可以写成：

$$
z = E(x)
$$

解码过程可以写成：

$$
\hat{x} = D(z)
$$

其中 $x$ 是原始视频， $z$ 是视频 latent， $\hat{x}$ 是重建或生成视频。

在训练或图生视频任务中，编码器用于把输入视频、参考图或首帧压缩成 latent；在生成阶段，扩散模型通常直接在 latent 空间中去噪，最后由解码器输出视频帧。

二者的职责可以对比如下：

| 模块 | 输入 | 输出 | 作用 |
|------|------|------|------|
| VAE Encoder | 原始图像/视频 | latent 表示 | 降低维度，提取紧凑表示 |
| VAE Decoder | 生成后的 latent | RGB 图像/视频 | 恢复像素内容，决定最终画质 |

VAE 质量非常关键。如果编码器压缩损失过大，细节会提前丢失；如果解码器重建能力弱，最终视频会模糊、纹理差、边缘不稳定。视频 VAE 还必须保证跨帧解码一致，否则会引入 flicker。

面试里可以回答：**VAE Encoder 负责把像素视频压缩成 latent，降低扩散建模成本；VAE Decoder 负责把生成 latent 解码回视频，决定最终清晰度和细节质量。视频 VAE 还要特别关注时间一致性。**

<h2 id="面试问题dit--transformer-主干网络如何处理时空信息">面试问题：DiT / Transformer 主干网络如何处理时空信息？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

DiT / Transformer 主干处理时空信息的核心方式是：**把视频 latent 切成时空 token，然后通过 attention 在 token 之间建立空间和时间依赖。**

假设视频 latent 为：

$$
z \in \mathbb{R}^{T' \times C_z \times H' \times W'}
$$

可以切成 patch token：

$$
z \rightarrow \{p_1, p_2, \dots, p_N\}
$$

其中 $N$ 和时间长度、空间分辨率、patch 尺寸有关。Transformer 通过 self-attention 让 token 之间交互：

$$
\mathrm{Attention}(Q,K,V)
= \mathrm{softmax}\left(\frac{QK^\top}{\sqrt{d}}\right)V
$$

在视频里，token 之间的关系既包括同一帧内部的空间关系，也包括不同帧之间的时间关系。常见实现包括：

1. **Full Space-Time Attention**：所有时空 token 一起做 attention，表达能力强但成本高。
2. **Factorized Attention**：先做空间 attention，再做时间 attention，降低复杂度。
3. **Window / Sparse Attention**：限制 attention 范围，适合长视频和高分辨率。
4. **3D RoPE / 时空位置编码**：注入时间、高度、宽度位置信息。
5. **Cross-Attention / AdaLN**：注入文本、图像、时间步和其他条件。

面试中可以总结为：**DiT / Transformer 通过把视频表示成时空 token，并用 self-attention、位置编码和条件调制来建模空间结构、时间运动和跨帧一致性。它的优势是长程依赖强，挑战是 token 数量大、计算成本高。**

<h2 id="面试问题噪声调度器在扩散模型中起什么作用">面试问题：噪声调度器在扩散模型中起什么作用？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

噪声调度器（Noise Scheduler）的作用是：**定义扩散模型在每个时间步加多少噪声，以及采样时如何从当前 noisy latent 更新到更干净的 latent。**

训练时，scheduler 定义前向加噪过程：

$$
z_t = \sqrt{\bar{\alpha}_t} z_0 + \sqrt{1-\bar{\alpha}_t}\epsilon
$$

采样时，scheduler 决定如何从 $z_t$ 更新到 $z_{t-1}$。不同采样器会有不同更新公式，例如 DDPM、DDIM、Euler、DPM-Solver 等。

噪声调度器影响几个关键因素：

| 影响项 | 说明 |
|--------|------|
| 生成质量 | 噪声步设计影响结构和细节恢复 |
| 推理速度 | 采样步数越少越快，但可能质量下降 |
| 稳定性 | 不同 scheduler 对伪影、过饱和、细节稳定性影响不同 |
| 可控性 | 与 CFG、条件强度、采样步数共同影响 prompt following |

在视频生成中，scheduler 还会影响时序稳定性。采样步数过少可能导致运动不自然或细节不稳定；步数过多则成本高。实际部署中经常需要在质量、速度和稳定性之间调参。

面试里可以回答：**噪声调度器定义扩散过程中的噪声强度和反向采样策略，是控制生成质量、速度和稳定性的关键模块。视频生成中 scheduler 选择还会影响跨帧一致性和推理成本。**

<h2 id="面试问题视频生成系统部署时常见的计算瓶颈有哪些">面试问题：视频生成系统部署时常见的计算瓶颈有哪些？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐ (3/5)**

视频生成系统部署时的计算瓶颈主要来自：**视频 token 数量大、扩散采样步数多、时空 attention 昂贵、VAE 解码和后处理成本高。**

常见瓶颈包括：

| 瓶颈 | 原因 | 优化方向 |
|------|------|----------|
| 显存占用 | 多帧、高分辨率、长序列 latent | latent 压缩、分块推理、低精度 |
| Attention 计算 | 时空 token 数量大，attention 成本高 | window attention、factorized attention、稀疏 attention |
| 采样步数 | 扩散模型需要多次迭代去噪 | DDIM、DPM-Solver、蒸馏、少步采样 |
| VAE 解码 | 多帧解码成本高 | tiled decoding、视频 VAE 优化 |
| 条件编码 | 多模态编码器增加延迟 | 缓存条件 embedding、轻量编码器 |
| 后处理 | 插帧、超分、去闪烁额外耗时 | 异步流水线、模型压缩、批处理 |

视频 token 数量可以粗略理解为：

$$
N \approx T \cdot \frac{H}{p_h} \cdot \frac{W}{p_w}
$$

如果使用 full attention，复杂度接近：

$$
\mathcal{O}(N^2)
$$

因此帧数、分辨率和 patch 尺寸都会显著影响部署成本。

常见优化方法包括 FP16/BF16/INT8、xFormers/FlashAttention、KV cache、模型蒸馏、LoRA 合并、分块 VAE 解码、低分辨率生成后超分、减少采样步数、并行化流水线等。

面试中可以总结为：**视频生成部署的主要瓶颈是显存、时空 attention、采样步数、VAE 解码和后处理。优化通常围绕压缩 latent、减少 token、减少采样步、使用高效 attention 和低精度推理展开。**
